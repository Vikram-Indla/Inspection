# Field-channel delta log

Format per `PLAN.md` non-negotiable #8: route · what differed · presentation-vs-data · resolution · token/class used.
One entry per resolved delta, appended during build. Empty until build starts.

- `/field/inspection/[id]` submission outbox · No call-site delta: the existing `submit_inspection` RPC call already matches `p_inspection`, `p_snapshot`, `p_idempotency_key`, `p_acknowledgement`; the DEC-029/032 search-path migration was source-verified, while local SQL dry-run was unavailable because this worktree has no `supabase/config.toml`/compose setup, no running Docker daemon, and no responding local PostgreSQL server · data · Kept the migration and `offline.ts` unchanged; did not contact or apply DDL to any shared/remote Supabase instance · N/A (no presentation token/class).
- `/field/inspection/[id]` report-kind boundary · Definitions carrying any truthy `package_kind` previously continued into visit-report item/checklist/library/violation reads even though only the no-`package_kind` visit-report renderer is configured · data · Added a fail-closed server-page return immediately after the frozen definition is resolved; no-`package_kind` definitions continue through the existing `FactoryVerification`/`Workspace` path unchanged · shared `EmptyState` (`ax-state`), no new class.
- `/api/shell/search` result links · The API's six real result types used console/admin destinations; the prior path-based client rewrite covered only CR-style hrefs and could produce a non-canonical field path · presentation/navigation (data and RLS-scoped search results unchanged) · Applied the exact six-type PLAN v7 matrix only when `isFieldOnlyPersona(roles)` is true; console, admin, and mixed-role sessions retain each API href byte-for-byte · existing `ax-shell-search__results`, no new class.
- `/field/notifications/[id]` + field inbox / global bell · Field notifications had only a summary/mark-read inbox, the field action updated legacy `delivery_state` without authoritative `read_at`, and bell planning links always targeted `/visits/:id` even for field-only sessions · data/read-receipt + presentation/navigation · Added a recipient/RLS-scoped detail read that renders only the stored `event_key`, `created_at`, and actual payload keys with a safe malformed-payload fallback; opening records the first `read_at` and queued rows also transition to `delivery_state='read'`; the field action shares that receipt patch; bell visit links delegate to item 1's `shellGlobalSearchHref` resolver so field-only users receive `/field/:id` while other personas retain the original web href · shared `ax-field-page`, `ax-surface`, `ax-panel`, `ax-banner`, `ax-link`, and semantic spacing/text tokens; no new class or raw visual token.
- `/field/establishments` (item 7 check a) · `/field/factory-360` was an identifier resolver only and no establishment list existed anywhere under `/field`; the sole browse/filter list was `/factories`, which the channel layout redirects an inspector-only persona away from · presentation/navigation over existing RLS-scoped master data · Added a paged, searchable field-native list over `factories` plus its real `industrial_licenses` relationship, with field-safe Factory 360 links only when the CR/licence mapping exists and an explicit unavailable state otherwise · shared `ax-field-page`, `ax-surface`, `ax-panel`, `ax-field--field`, `ax-lozenge`, and `ax-btn--field`; no new class or raw visual token.
- `/field/establishments` unregistered action (item 7 check b) · The real Immediate Visit path is not record-only: `planning/immediate/actions.ts` calls `create_immediate_visit`, whose transaction inserts a temporary `factories` row (`source='immediate_manual'`, `is_temporary=true`), a published `visits` row, and `assignments`, then redirects an inspector to `/field/:visitId` · data/workflow · Linked the field establishment surface to the existing `/planning/immediate` exception instead of creating a second action or a standalone unlicensed record; the established permission, duplicate, package, location, assignment, audit, and idempotency guards remain authoritative · shared `ax-btn--prominent ax-btn--field`; no planning/visits source changed.
- Field inspection export-products ownership (item 7 check c) · No `export_products`/equivalent field exists in `factories`, `factory_products`, `plant_production_line_items`, `checklist_responses`, or `inspection_factory_checks`; official product identity/capacity belongs to `factory_products` and versioned `plant_production_line_items`, while inspector-observed divergence is deliberately separate in `inspection_factory_checks(field_key, source_value, observed_value, status, evidence_note)` under FND-007 · data / human authority required · Whether “exports products” is an official master-data fact or an inspection observation is not determined by the schema, so no field or migration was added; the sub-item stops at this recorded ownership decision boundary · N/A (no presentation change).
- `/field/incident-reports` · The real `incident_reports` table and inspector insert RLS existed, but the only UI was `/incident-reports`; the authenticated layout redirects an inspector-only persona away from every non-`/field` route · presentation + data wiring · Added a separate field-native read/create route using the migration's exact ten text fields plus read-only real anchors/audit metadata; `report_source`, `incident_type`, `report_time`, and `number_of_cases` remain unconstrained text exactly as the migration requires, and the existing non-field route is untouched · shared `ax-field-page`, `ax-grid-2`, `ax-field--field`, `ax-surface`, `ax-panel`, `ax-banner`, `ax-lozenge`, and `ax-btn--field`; no new class or raw visual token.
- `/field` item 7 entry points · Field-only users had no visible route entry to establishment browsing or incident logging · presentation/navigation only · Added two touch-sized links to `/field/establishments` and `/field/incident-reports`; no dashboard data, KPI, assignment, workflow, or other PLAN item changed · shared `ax-surface`, `ax-panel`, `ax-btn--secondary`, and `ax-btn--field`; no new class.
- `/field/settings` (item 3) · Field-only users previously landed on the editable shared profile route and had no field-local language/theme, offline-state, or device-enrollment surface; the only device UI was the Ops/Security control plane · presentation + RLS-governed data write · Added a field settings route and account-menu entry with persisted EN/AR and existing `ThemeToggle`, a read-only snapshot of `offline.ts`'s exported outbox/conflict stores, and pending-only self-enrollment of the local `field-device.ts` identifier; the server derives the user and hardcodes every security-relevant inserted value, never updates on collision, and labels a self-enrolled row “Pending approval — this device is not trusted yet” until the backend actually grants `trusted` · shared `ax-field-page`, `ax-surface`, `ax-panel`, `ax-banner`, `ax-lozenge`, `ax-btn--field`, and semantic tokens; no new raw visual token or destructive storage control.
- `/login` → `/reset` password recovery (item 8) · The global reset flow requested a Supabase magic link with `redirectTo=/reset`, while `/reset` waited for a URL-fragment `PASSWORD_RECOVERY` event or any already-established session · authentication/workflow · Kept the neutral `resetPasswordForEmail(email)` request and both existing FND-003 `logAuthEvent` calls, replaced the link handoff with a six-digit `verifyOtp({ email, token, type: "recovery" })` step, and gated the existing `updateUser({ password })` form on the matching same-tab recovery session before signing it out; known and unknown request outcomes render identical anti-enumerating copy. The hosted recovery email template must contain `{{ .Token }}` and remains a real-project QA check because repository source cannot verify hosted template configuration · existing `lg-card`, `lg-credentials`, `ax-field`, `ax-input`, `ax-banner`, and `btn` classes; no new class or raw visual token.
- Field offline storage and replay (item 6) · The shared `mim-field-v1` IndexedDB exposed drafts, packages, outbox operations, and conflicts across sequential users on one browser, while replay used the mutable Supabase browser singleton and re-read identity during individual operations · authentication/data isolation · Replaced every field caller with the single `localForUser(serverVerifiedUserId)` accessor over `mim-field-v1:${userId}`; added one browser-wide explicit restore/decline prompt that reads the retired database without ever writing it; captured and verified one replay user/token/store context, used a throwaway Supabase client with the root `accessToken` callback, guarded every network call and every processed-entry removal against the live session, and sourced all replay identity/audit columns from the captured ID. Idempotency, explicit conflicts, workflow RPCs, and RLS remain unchanged · no presentation class for storage/replay.
- `/field` drafts and connectivity (item 6) · Pending attention previously listed only server `in_progress` rows and the sync chip could report online/offline but no evidence-based weak state · presentation over user-scoped local data + governed server data · Added a client union of the current user's local draft inspection IDs and server `in_progress` inspections, deduped by `inspection_id` with server labels winning; added a warning banner only for browser-reported `slow-2g`/`2g`, an offline banner from `navigator.onLine`, and no weak claim when `navigator.connection` is absent (Safari/iOS fallback) · shared `ax-banner--warning`, `ax-lozenge--warning`, `ax-btn--subtle`, and semantic spacing; no thresholds or raw visual tokens introduced.
