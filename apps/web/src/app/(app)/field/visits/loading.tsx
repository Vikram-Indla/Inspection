export default function Loading() {
  return <main style={{ padding: 20 }} aria-busy="true" aria-label="Loading assigned visits">
    <div className="panel" style={{ minHeight: 180 }} />
  </main>;
}
