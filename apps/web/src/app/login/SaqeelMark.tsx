// Saqeel logomark — shield with checkmark. Re-exports the single canonical
// brand mark (see components/SaqeelBrandMark.tsx) so login, the console nav
// drawer header, and Sidebar.tsx all render the exact same icon.
export { default } from "@/components/SaqeelBrandMark";
