# Screen build spec

Generated by `docs/design/figma/handoff/build-screen-spec.py`. One section per
in-scope governed screen.

**How to use this.** Open the Figma frame named in *Figma frame*, and this section
beside it. The frame is the visual contract; everything below is the part of the
contract a picture cannot carry. Where the two disagree, this file is wrong and the
catalogue wins — regenerate rather than editing by hand.

**Rules that override anything a frame appears to show** (`CLAUDE.md`):

- No new CSS, no new tokens. Every element renders with a class that already exists
  in `apps/web/src/app/saqeel-components.css`. A missing class is a design-system
  change request, not a page-level fix.
- Status is text plus shape, never colour alone.
- RTL via logical properties only — `padding-inline`, `margin-inline-start`,
  `inset-inline-start`, `border-inline-end`. Never `left`/`right`.
- Arabic comes from the i18n layer, never from a component.
- Never invent a governed value. Absent data renders as a state: *Not configured* /
  *Unavailable* / *Insufficient evidence*.

---

## SCR-ADM-001 — Admin Home

- **Route:** `/admin`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-001 — Admin Home` on page `Admin Shell`
- **Personas:** Compliance Admin; Business Admin; Technical Admin
- **Permission:** Role-scoped
- **Requirements riding on this screen:** 18 (4 strong)

**Purpose.** Configuration health, drafts, approvals, published versions, alerts and audit

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Admin dashboard
- engine health
- pending tasks
- recent publish
- dependency warnings

**Primary actions.**

- Open engine
- review draft
- approve
- publish
- inspect audit

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-001 — Admin Home — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Empty | `STATE: Empty` |
| loading | `STATE: loading` |
| service-degraded | `STATE: service-degraded` |
| unauthorized | `STATE: unauthorized` |

## SCR-ADM-010 — Regulation Library

- **Route:** `/admin/regulations`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-010 — Regulation Library` on page `Admin Shell`
- **Personas:** Compliance Admin
- **Permission:** Compliance Admin
- **Requirements riding on this screen:** 83 (75 strong)

**Purpose.** Manage regulation hierarchy, clauses, effective dates and versions

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Search
- filters
- regulation list
- version/status
- owner
- effective date

**Primary actions.**

- Create draft
- open
- clone
- archive
- submit review

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-010 — Regulation Library — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| No records | `STATE: No records` |
| validation errors | `STATE: validation errors` |
| publish blocked | `STATE: publish blocked` |

## SCR-ADM-011 — Regulation Detail & Version

- **Route:** `/admin/regulations/[id]`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-011 — Regulation Detail & Version` on page `Admin Shell`
- **Personas:** Compliance Admin; Reviewer
- **Permission:** Maker-checker
- **Requirements riding on this screen:** 9 (4 strong)

**Purpose.** Edit clauses, applicability, legal reference and lifecycle

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Metadata
- clauses
- mappings
- dependencies
- versions
- audit

**Primary actions.**

- Edit draft
- validate
- compare
- submit
- approve
- publish

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-011 — Regulation Detail & Version — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Locked published version | `STATE: Locked published version` |
| dependency conflict | `STATE: dependency conflict` |

## SCR-ADM-020 — Inspection Item Catalogue

- **Route:** `/admin/items`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-020 — Inspection Item Catalogue` on page `Admin Shell`
- **Personas:** Compliance Admin; Form Admin
- **Permission:** Config role
- **Requirements riding on this screen:** 189 (126 strong)

**Purpose.** Define items, response types, guidance and runtime meaning

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Item code
- title
- response model
- required rule
- scoring
- evidence

**Primary actions.**

- Create
- edit draft
- map clause
- deactivate

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-020 — Inspection Item Catalogue — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Duplicate code | `STATE: Duplicate code` |
| invalid mapping | `STATE: invalid mapping` |

## SCR-ADM-030 — Inspection Package Library

- **Route:** `/admin/packages`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-030 — Inspection Package Library` on page `Admin Shell`
- **Personas:** Form Admin; Compliance Admin
- **Permission:** Maker-checker
- **Requirements riding on this screen:** 32 (26 strong)

**Purpose.** Manage package versions used by visits and inspections

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Package list
- scope
- status
- active version
- dependencies

**Primary actions.**

- Create
- clone
- preview
- validate
- publish

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-030 — Inspection Package Library — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Unresolved item | `STATE: Unresolved item` |
| active dependency | `STATE: active dependency` |

## SCR-ADM-031 — Package & Form Designer

- **Route:** `/admin/packages/[id]/designer`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-031 — Package & Form Designer` on page `Admin Shell`
- **Personas:** Form Admin
- **Permission:** Form Admin
- **Requirements riding on this screen:** 71 (50 strong)

**Purpose.** Compose sections, items, conditions, evidence rules and action-form triggers

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Section tree
- canvas
- item inspector
- rules
- preview
- validation

**Primary actions.**

- Add/reorder
- configure condition
- simulate
- save draft

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-031 — Package & Form Designer — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Circular logic | `STATE: Circular logic` |
| missing response meaning | `STATE: missing response meaning` |
| publish blocker | `STATE: publish blocker` |

## SCR-ADM-040 — Violation Catalogue

- **Route:** `/admin/violations`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-040 — Violation Catalogue` on page `Admin Shell`
- **Personas:** Compliance Admin
- **Permission:** Compliance Admin
- **Requirements riding on this screen:** 202 (115 strong)

**Purpose.** Manage violation codes, levels, legal basis and applicability

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Violation list
- level
- category
- active dates
- source clause

**Primary actions.**

- Create
- map
- version
- deactivate

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-040 — Violation Catalogue — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Duplicate or orphan violation | `STATE: Duplicate or orphan violation` |

## SCR-ADM-041 — Penalty Mapping

- **Route:** `/admin/penalties`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-041 — Penalty Mapping` on page `Admin Shell`
- **Personas:** Compliance Admin; Approver
- **Permission:** Maker-checker
- **Requirements riding on this screen:** 69 (67 strong)

**Purpose.** Map violations to penalty records/ranges and cardinality rules

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Violation
- penalty
- range
- repeat rule
- one-to-one rule
- version

**Primary actions.**

- Create mapping
- validate
- publish

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-041 — Penalty Mapping — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Overlapping mapping | `STATE: Overlapping mapping` |
| missing legal basis | `STATE: missing legal basis` |

## SCR-ADM-050 — Workflow Library

- **Route:** `/admin/workflows`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-050 — Workflow Library` on page `Admin Shell`
- **Personas:** Workflow Admin
- **Permission:** Workflow Admin
- **Requirements riding on this screen:** 1 (1 strong)

**Purpose.** Manage workflow definitions and active versions

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Workflow list
- object
- status
- version
- effective date

**Primary actions.**

- Create
- clone
- test
- publish

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-050 — Workflow Library — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Missing transition/guard | `STATE: Missing transition/guard` |

## SCR-ADM-051 — Workflow Designer

- **Route:** `/admin/workflows/[id]`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-051 — Workflow Designer` on page `Admin Shell`
- **Personas:** Workflow Admin; Approver
- **Permission:** Maker-checker
- **Requirements riding on this screen:** 39 (2 strong)

**Purpose.** Configure states, transitions, roles, guards, SLA and notifications

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- State canvas
- transition panel
- guard builder
- side effects
- simulation

**Primary actions.**

- Add state
- connect
- configure
- validate
- simulate
- publish

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-051 — Workflow Designer — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Unreachable state | `STATE: Unreachable state` |
| unauthorized transition | `STATE: unauthorized transition` |
| circular terminal path | `STATE: circular terminal path` |

## SCR-ADM-060 — Risk Configuration

- **Route:** `/admin/risk`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-060 — Risk Configuration` on page `Admin Shell`
- **Personas:** Risk Owner; Compliance Admin
- **Permission:** Risk Owner
- **Requirements riding on this screen:** 43 (23 strong)

**Purpose.** Configure approved MVP1 risk inputs, thresholds and version metadata

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Drivers
- weights/formula reference
- thresholds
- source
- effective version

**Primary actions.**

- Edit draft
- test sample
- approve
- publish

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-060 — Risk Configuration — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Unknown formula/weights remain decision-blocked | `STATE: Unknown formula/weights remain decision-blocked` |

## SCR-ADM-070 — GIS & Geofence Settings

- **Route:** `/admin/gis`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-070 — GIS & Geofence Settings` on page `Admin Shell`
- **Personas:** GIS Admin; Technical Admin
- **Permission:** GIS Admin
- **Requirements riding on this screen:** 2 (0 strong)

**Purpose.** Manage factory coordinates, geofence rules, arrival thresholds and override policy

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Map layers
- geofence type
- radius/polygon
- accuracy
- override roles

**Primary actions.**

- Edit
- validate
- preview
- publish

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-070 — GIS & Geofence Settings — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Invalid geometry | `STATE: Invalid geometry` |
| threshold unresolved | `STATE: threshold unresolved` |

## SCR-ADM-080 — Notification & SLA Rules

- **Route:** `/admin/notifications`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-080 — Notification & SLA Rules` on page `Admin Shell`
- **Personas:** Business Admin; Technical Admin
- **Permission:** Maker-checker
- **Requirements riding on this screen:** 8 (1 strong)

**Purpose.** Configure events, recipients, templates, timers and escalation

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Event catalogue
- templates
- recipients
- channels
- SLA
- escalation

**Primary actions.**

- Create
- test
- activate
- deactivate

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-080 — Notification & SLA Rules — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Missing recipient | `STATE: Missing recipient` |
| provider unavailable | `STATE: provider unavailable` |

## SCR-ADM-090 — Roles & Permissions

- **Route:** `/admin/access`
- **Channel:** Admin Portal · **Process:** G2-P00
- **Figma frame:** `SCR-ADM-090 — Roles & Permissions` on page `Admin Shell`
- **Personas:** Security Admin
- **Permission:** Security Admin
- **Requirements riding on this screen:** 1 (1 strong)

**Purpose.** Manage role permissions and data scope

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Role matrix
- channel
- object
- action
- scope
- segregation rules

**Primary actions.**

- Create role
- assign permission
- compare
- publish

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-ADM-090 — Roles & Permissions — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Privilege conflict | `STATE: Privilege conflict` |
| segregation violation | `STATE: segregation violation` |

## SCR-VIR-700 — Virtual Appointment & Waiting Room

- **Route:** `/virtual/appointments/[id]`
- **Channel:** Virtual · **Process:** G2-P06B
- **Figma frame:** `SCR-VIR-700 — Virtual Appointment & Waiting Room` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Inspector; Factory Representative
- **Permission:** Participant/Inspector
- **Requirements riding on this screen:** 11 (11 strong)

**Purpose.** Join governed virtual inspection

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Appointment
- participants
- instructions
- device readiness
- join controls

**Primary actions.**

- Join
- admit
- reschedule

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-VIR-700 — Virtual Appointment & Waiting Room — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Wrong appointment | `STATE: Wrong appointment` |
| late | `STATE: late` |
| device failure | `STATE: device failure` |

## SCR-VIR-710 — Identity & OTP Verification

- **Route:** `/virtual/sessions/[id]/verify`
- **Channel:** Virtual · **Process:** G2-P06B
- **Figma frame:** `SCR-VIR-710 — Identity & OTP Verification` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Inspector; Factory Representative; System
- **Permission:** Participant/System
- **Requirements riding on this screen:** 12 (7 strong)

**Purpose.** Verify participants and unlock session

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Identity details
- OTP state
- retries
- exception
- audit

**Primary actions.**

- Verify
- resend OTP
- escalate

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-VIR-710 — Identity & OTP Verification — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| OTP failure | `STATE: OTP failure` |
| identity mismatch | `STATE: identity mismatch` |
| provider unavailable | `STATE: provider unavailable` |

## SCR-VIR-720 — Virtual Inspection Session

- **Route:** `/virtual/[id]`
- **Channel:** Virtual · **Process:** G2-P07
- **Figma frame:** `SCR-VIR-720 — Virtual Inspection Session` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Inspector; Factory Representative
- **Permission:** Inspector
- **Requirements riding on this screen:** 252 (204 strong)

**Purpose.** Conduct remote inspection and capture evidence

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Video
- participant list
- agenda
- evidence
- notes
- checklist
- status

**Primary actions.**

- Capture
- request view
- add evidence
- close

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-VIR-720 — Virtual Inspection Session — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Connectivity failure | `STATE: Connectivity failure` |
| insufficient evidence | `STATE: insufficient evidence` |
| fallback physical | `STATE: fallback physical` |

## SCR-WEB-100 — Visit Planning Home

- **Route:** `/planning`
- **Channel:** Web Portal · **Process:** G2-P01
- **Figma frame:** `SCR-WEB-100 — Planning — /planning` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner
- **Permission:** Planner
- **Requirements riding on this screen:** 34 (33 strong)

**Purpose.** Start Bulk, Single or Immediate planning and view drafts

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Planning method cards
- drafts
- recent plans
- alerts

**Primary actions.**

- Create plan
- resume draft

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-100 — Visit Planning Home — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| No permission | `STATE: No permission` |
| no package | `STATE: no package` |

## SCR-WEB-110 — Bulk Planning Criteria

- **Route:** `/planning/bulk`
- **Channel:** Web Portal · **Process:** G2-P01
- **Figma frame:** `SCR-WEB-110 — Bulk Planning Criteria` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner
- **Permission:** Planner
- **Requirements riding on this screen:** 42 (17 strong)

**Purpose.** Build criteria, retrieve factories and select targets

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Filter builder
- AND/OR
- result count
- factory table
- selection

**Primary actions.**

- Search
- select
- save criteria
- continue

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-110 — Bulk Planning Criteria — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| No matches | `STATE: No matches` |
| invalid condition | `STATE: invalid condition` |
| data source unavailable | `STATE: data source unavailable` |

## SCR-WEB-120 — Single Visit Planning

- **Route:** `/planning/single`
- **Channel:** Web Portal · **Process:** G2-P01
- **Figma frame:** `SCR-WEB-120 — Single Visit Planning` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner
- **Permission:** Planner
- **Requirements riding on this screen:** 75 (72 strong)

**Purpose.** Find one registered factory and configure visit

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Factory search
- Factory 360 preview
- visit configuration

**Primary actions.**

- Select factory
- configure
- assign

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-120 — Single Visit Planning — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Factory not found | `STATE: Factory not found` |
| duplicate visit | `STATE: duplicate visit` |

## SCR-WEB-130 — Immediate Visit Planning

- **Route:** `/planning/immediate`
- **Channel:** Web Portal · **Process:** G2-P01
- **Figma frame:** `SCR-WEB-130 — Immediate Visit Planning` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner; Authorized Inspector
- **Permission:** Restricted role
- **Requirements riding on this screen:** 115 (66 strong)

**Purpose.** Create urgent registered/unregistered visit with minimum controls

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Urgency reason
- identity/location
- visit type
- assignment

**Primary actions.**

- Create
- validate
- dispatch

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-130 — Immediate Visit Planning — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Missing minimum identity/location | `STATE: Missing minimum identity/location` |
| unauthorized | `STATE: unauthorized` |

## SCR-WEB-140 — Visit Configuration & Assignment

- **Route:** `/planning/bulk/review`
- **Channel:** Web Portal · **Process:** G2-P02
- **Figma frame:** `SCR-WEB-140 — Visit Configuration & Assignment` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner
- **Permission:** Planner
- **Requirements riding on this screen:** 34 (34 strong)

**Purpose.** Set visit window, type, package, mode and assignment

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Configuration form
- package version
- mode
- team
- conflict panel

**Primary actions.**

- Assign auto/manual
- resolve conflict
- review

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-140 — Visit Configuration & Assignment — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| No package | `STATE: No package` |
| unavailable inspector | `STATE: unavailable inspector` |
| overlapping visit | `STATE: overlapping visit` |

## SCR-WEB-150 — Plan Review & Publish

- **Route:** `/planning/[id]/review`
- **Channel:** Web Portal · **Process:** G2-P03
- **Figma frame:** `SCR-WEB-150 — Plan Review & Publish` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner; Approver if configured
- **Permission:** Planner
- **Requirements riding on this screen:** 62 (32 strong)

**Purpose.** Validate and publish plan and child visits

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Summary
- targets
- visits
- assignments
- validation
- notifications

**Primary actions.**

- Publish
- return to edit

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-150 — Plan Review & Publish — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Validation failure | `STATE: Validation failure` |
| stale data | `STATE: stale data` |
| duplicate | `STATE: duplicate` |

## SCR-WEB-200 — Visit Management Workspace

- **Route:** `/visits`
- **Channel:** Web Portal · **Process:** G2-P03
- **Figma frame:** `SCR-WEB-200 — Visit Management Workspace` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner; Operations User; Branch Manager
- **Permission:** Role-scoped
- **Requirements riding on this screen:** 30 (13 strong)

**Purpose.** Search, filter and operate on visits and plans

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- KPI cards
- table
- filters
- saved views
- map/calendar toggle
- bulk bar

**Primary actions.**

- Open
- edit
- reassign
- cancel
- return
- export
- bulk action

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-200 — Visit Management Workspace — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Partial bulk failure | `STATE: Partial bulk failure` |
| stale row | `STATE: stale row` |
| unauthorized | `STATE: unauthorized` |

## SCR-WEB-210 — Visit Detail

- **Route:** `/visits/[id]`
- **Channel:** Web Portal · **Process:** G2-P03
- **Figma frame:** `SCR-WEB-210 — Visit Detail` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner; Operations; Inspector read context
- **Permission:** Role/state scoped
- **Requirements riding on this screen:** 37 (20 strong)

**Purpose.** View complete visit, assignment, timeline, package and exceptions

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Header
- status
- factory
- assignment
- schedule
- package
- timeline
- map

**Primary actions.**

- Edit allowed fields
- reassign
- cancel
- open Factory 360

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-210 — Visit Detail — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Locked state | `STATE: Locked state` |
| conflict | `STATE: conflict` |
| not found | `STATE: not found` |

## SCR-WEB-300 — Level 2 Review Queue

- **Route:** `/reviews`
- **Channel:** Web Portal · **Process:** G2-P10
- **Figma frame:** `SCR-WEB-300 — Review & Approval — /reviews` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Level 2 Reviewer
- **Permission:** Reviewer
- **Requirements riding on this screen:** 35 (13 strong)

**Purpose.** Prioritize submitted inspections by SLA, risk and status

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Queue
- filters
- SLA
- risk
- assignee
- version
- workload

**Primary actions.**

- Claim/open/reassign

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-300 — Level 2 Review Queue — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| No evidence | `STATE: No evidence` |
| stale assignment | `STATE: stale assignment` |
| unauthorized | `STATE: unauthorized` |

## SCR-WEB-310 — Level 2 Review Workspace

- **Route:** `/reviews/[id]`
- **Channel:** Web Portal · **Process:** G2-P10
- **Figma frame:** `SCR-WEB-310 — Level 2 Review Workspace` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Level 2 Reviewer
- **Permission:** Reviewer
- **Requirements riding on this screen:** 66 (28 strong)

**Purpose.** Review immutable submission and decide

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Read-only report
- checklist
- evidence
- findings
- violations
- timeline
- decision panel

**Primary actions.**

- Approve
- return
- reject
- add immutable comments

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-310 — Level 2 Review Workspace — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Missing reason | `STATE: Missing reason` |
| stale version | `STATE: stale version` |
| unauthorized | `STATE: unauthorized` |

## SCR-WEB-320 — Version Comparison

- **Route:** `/reviews/[id]`
- **Channel:** Web Portal · **Process:** G2-P11
- **Figma frame:** `SCR-WEB-320 — Version Comparison` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Reviewer; Auditor
- **Permission:** Reviewer/Auditor
- **Requirements riding on this screen:** 47 (28 strong)

**Purpose.** Compare returned/resubmitted versions (route-neutral compare mode; CC-CD030-ROUTE-001)

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Section diff
- field diff
- evidence diff
- comments
- version metadata

**Primary actions.**

- Navigate changed sections (Accept changes deferred — CC-CD030-ROUTE-001)

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-320 — Version Comparison — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Missing version | `STATE: Missing version` |
| incompatible package | `STATE: incompatible package` |

## SCR-WEB-400 — Factory 360

- **Route:** `/factories/[id]/360`
- **Channel:** Web Portal · **Process:** G2-P12
- **Figma frame:** `SCR-WEB-400 — Factory 360` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Planner; Inspector; Reviewer; Operations; Leadership read
- **Permission:** Role/data-scope
- **Requirements riding on this screen:** 168 (82 strong)

**Purpose.** Unified factory identity, history, risk, findings, actions and documents

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Profile
- license
- CR
- location
- metrics
- history
- findings
- docs
- timeline

**Primary actions.**

- Open report
- filter history
- open map
- open action

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-400 — Factory 360 — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Subservice degraded | `STATE: Subservice degraded` |
| stale source | `STATE: stale source` |

## SCR-WEB-500 — Operations Center

- **Route:** `/operations`
- **Channel:** Operations Center · **Process:** G2-P12
- **Figma frame:** `SCR-WEB-500 — Operations Center — /operations` on page `— SCREENS —`, section `SCREENS — EN · Light`
- **Personas:** Operations User; Supervisor; Leadership
- **Permission:** Operations
- **Requirements riding on this screen:** 34 (14 strong)

**Purpose.** Monitor visits, journeys, SLA, alerts, overrides and workload

**Mandatory regions.** Every one must render. A region with no governed data
renders its state, not an empty div.

- Live map/list
- KPIs
- alerts
- workload
- exceptions
- tracking history

**Primary actions.**

- Open visit
- resolve alert
- approve override
- export

**States.** Each has its own frame in `SCREENS — STATES · EN · Light`,
named `SCR-WEB-500 — Operations Center — … — STATE: <state>`.

| State | Frame suffix |
|---|---|
| Widget fault | `STATE: Widget fault` |
| stale telemetry | `STATE: stale telemetry` |
| provider failure | `STATE: provider failure` |

---

## Figma → React

Full map in `component-map.json`. The entries a screen most often needs:

| Figma | React | Kind |
|---|---|---|
| `App sidebar` | `components/saqeel/navigation/Sidebar` | component |
| `App sidebar — Administration expanded` | `components/saqeel/navigation/Sidebar` | component |
| `App topbar` | `components/saqeel/navigation/TopBar` | component |
| `Brand lockup` | `components/saqeel/navigation/TopBar` | composed |
| `Brand mark` | `components/saqeel/SaqeelBrandMark` | component |
| `nav-item` | `components/saqeel/navigation/Sidebar` | composed |
| `Button` | `components/saqeel/actions/Button` | component |
| `Input` | `components/saqeel/inputs/Input` | component |
| `Select` | `components/saqeel/inputs/Select` | component |
| `Field` | `components/saqeel/inputs/Field` | component |
| `seg-opt` | `components/saqeel/inputs/SegmentedControl` | composed |
| `filter-chip` | `components/saqeel/navigation/FilterBar` | composed |
| `Badge` | `components/saqeel/data/StatusBadge` | component |
| `Panel` | — | composed |
| `KPI panel` | `components/saqeel/data/KPICard` | component |
| `stat` | `components/saqeel/data/MetricStrip` | composed |
| `Accordion header` | `components/saqeel/data/Accordion` | component |
| `DetailRow` | `components/saqeel/data/DetailRow` | component |
| `Table cell` | `components/saqeel/grid/DataGrid` | composed |
| `section-title` | — | composed |
| `Alert` | `components/saqeel/feedback/Alert` | component |
| `ad-state` | `components/saqeel/feedback/StateSurface` | component |
| `empty` | `components/saqeel/feedback/EmptyState` | component |
| `saqeel-state--loading` | `components/saqeel/feedback/Skeleton` | component |
| `saqeel-state--unauthorized` | `components/saqeel/feedback/StateSurface` | component |
| `saqeel-state--rls-denied` | `components/saqeel/feedback/StateSurface` | component |
| `saqeel-state--not-yet` | `components/saqeel/feedback/StateSurface` | component |
| `InspectionCard` | `components/saqeel/inspection/InspectionCard` | component |
| `Factory card` | `components/saqeel/inspection/InspectionCard` | component |
| `ReviewPanel` | `components/saqeel/inspection/ReviewPanel` | component |
| `FindingCard` | `components/saqeel/inspection/FindingCard` | component |
| `EvidenceCard` | `components/saqeel/inspection/EvidenceCard` | component |
| `EvidenceStack row` | `components/saqeel/signature/EvidenceStack` | composed |
| `DiffView` | `components/saqeel/feedback/DiffView` | component |
| `diff-row` | `components/saqeel/feedback/DiffView` | composed |
| `SeverityIndicator` | `components/saqeel/inspection/SeverityIndicator` | component |
| `DueDate` | `components/saqeel/inspection/DueDate` | component |
| `ChecklistQuestion` | `components/saqeel/inspection/ChecklistQuestion` | component |
| `ComplianceScore` | `components/saqeel/inspection/ComplianceScore` | component |
| `SyncIndicator` | `components/saqeel/feedback/SyncIndicator` | component |
| `GeoWorkspace` | `components/saqeel/signature/GeoWorkspace` | component |
| `ExceptionMark` | `components/saqeel/signature/ExceptionRail` | composed |

### Not a component

`panel-content/*` blocks are per-screen content, not reusable components. Build
them as JSX inside the route. The name after the slash is screen + region.

