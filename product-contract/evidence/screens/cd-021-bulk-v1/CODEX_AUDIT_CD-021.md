# Independent Codex Wiring Audit — CD-021 (Bulk Targeting, SCR-WEB-110 /planning/bulk + P02 /planning/bulk/review)

- reviewer: independent audit agent (Code Reviewer role), invoked specifically to perform the DEC-012 CD-021 wiring audit. Not the implementer of CD-021, no prior turns in this session touched `apps/web/src/app/planning/bulk/*`, `supabase/migrations/0026_cd021_bulk_publish_atomic.sql`, or any CD-021 output file.
- date: 2026-07-14
- branch: `feat/cd-022-single-identity-lens` (confirmed via `git branch --show-current`)
- commit-state: HEAD is `112f820 fix(cd-023): make immediate visits atomic`. **Correction to the record**: CD-021's actual implementation (`apps/web/src/app/planning/bulk/*`, `supabase/migrations/0026_cd021_bulk_publish_atomic.sql`, `apps/web/e2e/cd-021-bulk-targeting.spec.ts`) is **already committed** at `ea22b20 "feat: shared shell reconciliation, dashboard, KPI seed fixture, CD-021 bulk targeting"` (one commit behind HEAD; commit message itself states "CD-021 Bulk Targeting (SCR-WEB-110/120): ... P02 review route relocation" and "No push/merge to main"). Confirmed `ea22b20` is **not** an ancestor of `setup/Inspection` (`git rev-list setup/Inspection` does not contain it) — it exists only on the three local feature branches (`feat/cd-021-bulk-targeting`, `feat/cd-022-single-identity-lens`, `feat/cd-023-immediate-authority-bar`), so the no-push/no-merge-to-main rule is **not** violated. But this contradicts the repeated "uncommitted... nothing merged... no commit occurred" framing in `product-contract/evidence/screens/cd-021-bulk-v1/CD-021_EVIDENCE.md` and several `CURRENT_STATE.md` UPDATE entries — CD-021 code has in fact been sitting in a real commit since 2026-07-13. Only the evidence PNGs and `outputs/cd-021/*` manifest/CSV files remain as uncommitted/untracked working-tree changes at audit time (`git status --short` for the bulk paths shows just the 3 evidence PNGs modified and `outputs/cd-021/` untracked).
- **verdict: FAIL** — 8 of 13 wiring-map rows are FAIL, 2 are PASS, 3 are correctly BLOCKED_UPSTREAM. The core targeting mechanics (nested criteria evaluation, eligibility ledger, selection persistence/invalidation, and — most importantly — the atomic publish RPC) are real, correctly wired, and independently re-verified live. But the wiring map itself is stale relative to the current runtime (it predates the P02 relocation and the atomicity fix, both committed the same day), several of its specific negative-path claims do not exist in the shipped code, one row's automated-test claim cites a test file that does not exist, and one architectural change (the P02 relocation) shipped without the human approval its own manifest said it required.

## Gate effect

This file is the recorded independent audit DEC-012 requires (`CODEX_WIRING_AUDIT_CHECKLIST.md` → companion file under `${INSPECTION_DOCS_ROOT}/07_TEST_EVIDENCE_AND_SCREENSHOTS/product-contract/evidence/screens/<slice>/`). It satisfies the *audit-has-run* precondition. It does **not** satisfy the *slice-may-close* bar: per the checklist's verdict rollup, "FAIL — list the failing rows; slice returns to implementation, not closure." CD-021 may not be marked complete, closed, or sponsor-runtime-accepted until the 8 FAIL rows below are either fixed in code or the wiring map is corrected to truthfully describe what shipped (with any intentionally-deferred behavior recorded via `HANDOFF_BLOCKED` + a decision ID, not left as an unverified claim). Sponsor runtime acceptance remains separately outstanding regardless of this audit's outcome.

## Independent re-verification performed

- `cd apps/web && npm run typecheck` — **PASS**, zero errors.
- `cd apps/web && npm run build` — **PASS**, production build compiled; both `/planning/bulk` (3.82 kB) and `/planning/bulk/review` (2.32 kB) generated.
- Color-law grep (`grep -rn ... apps/web/src/app/planning/bulk/`) — **zero matches**, clean.
- Live Playwright (`PLAYWRIGHT_PORT=3997 PLAYWRIGHT_REUSE_SERVER=0 npx playwright test cd-021-bulk-targeting.spec.ts --project=setup --project=e2e`), fresh production build, real seeded Supabase personas: **PASS (15) FAIL (0)**, 44.6s. Reconciles exactly: 3 persona auth-setup tests + 12 tests actually present in `cd-021-bulk-targeting.spec.ts` (counted directly: `grep -c "^\s*test(" ` → 12) = 15. This is a genuine independent run, not a restatement of the implementer's claim.
- **Atomicity — independently forced, not read from a comment.** Extracted the live planner session token from the Playwright auth storage state (`playwright/.auth/planner.json`, cookie `sb-iiozvqntawxfwbgffzqu-auth-token`) and called `POST /rest/v1/rpc/publish_bulk_plan` directly via PostgREST with a real factory id but a **bogus `p_package_version_id`** (`00000000-0000-0000-0000-000000000000`). Result: HTTP 409, `23503` FK violation on `visits_package_version_id_fkey`, exactly as the evidence file describes. Counted `visit_plans` where `method=eq.bulk` before (8) and after (8) via the REST count header — **unchanged**, confirming the entire multi-statement write (plan insert, visit insert, assignment insert, two status updates, notification insert) rolled back atomically with zero orphan rows. This independently confirms `supabase/migrations/0026_cd021_bulk_publish_atomic.sql` is a real transaction, not a comment claiming one — the exact defect (`WIRING_MAP_CD-021.csv` row 10's own text: "actions.ts full read 2026-07-13: 6 sequential writes; comment claims atomic but none exists") that prompted migration 0026 in the first place has been fixed and is now proven live.
- Confirmed via `supabase/migrations/0002_rbac_audit.sql:20-27` that `visit_plans`/`visits` inserts require `has_role('planner')` and `assignments` inserts require `has_any_role(['planner','ops'])`, and via `0002_rbac_audit.sql:142-156` that an append-only (`revoke update, delete on audit_events`, `0001_foundation.sql:308`) trigger `trg_audit_visit_plans`/`trg_audit_visits`/`trg_audit_assignments` fires automatically inside the same transaction as `publish_bulk_plan`'s writes — so a successful publish is genuinely audited, and a rolled-back attempt leaves no false audit trail either.
- Confirmed `notifications.delivery_state` defaults to `'queued'` (`0001_foundation.sql:316`) — `publish_bulk_plan`'s notification insert (migration line 91-96) never claims "delivered."

## Per-row wiring-map verification

| # | ui_trigger | Claim (`WIRING_MAP_CD-021.csv`) | Verdict | Evidence |
|---|---|---|---|---|
| 1 | Apply criteria | Server drops unknown/empty values; success: table+ledger+distributions re-render with counts; negative: "Invalid condition → role=alert summary; results frozen on last valid criteria" | **FAIL** (success path CONFIRMED; negative path does not exist) | Success: `page.tsx:24-26,35-37` (`parseCt`→`fromFlat`→`emptyTree`, then `evalNode` filter) and `EligibilityLedger`/`DistributionPanels` genuinely reflect the filtered set — exercised live (Playwright "narrows eligible below denominator" test passes, 10/59 high-risk). **But** the claimed negative path is fabricated: `grep -n "alert" CriteriaBuilder.tsx` → 0 matches; a malformed/garbage `ct` param is silently dropped by `criteria.ts:fromWire` (returns `null` on any unrecognized shape) and `parseCt` falls through to `emptyTree()` — i.e. it silently **matches everything**, not "frozen on last valid criteria," and shows no alert of any kind. |
| 2 | Focus condition | "Contribution highlight in ledger/distribution/table"; reduced-motion instant highlight; row self-labeled "DESIGN-NEW (no runtime leg; pure presentation)" | **FAIL** (feature does not exist in the runtime at all) | `grep -n "focus" CriteriaBuilder.tsx BulkForm.tsx EligibilityLedger.tsx DistributionPanels.tsx` → 0 matches for any highlight/focus logic. `EligibilityLedger.tsx`'s `LedgerStrings.focusContribution` field is declared (`page.tsx:81`) but never rendered anywhere in `EligibilityLedger.tsx`'s JSX — a dead string. The row is self-labeled a design-only note, but it specifies concrete success/negative behavior as if it were a real deliverable and is not carried in `removals` or backed by a `HANDOFF_BLOCKED` decision — it is simply an unbuilt claim. |
| 3 | Add/edit/remove/reorder condition | Tree updates; announce count on apply; negative: "Unsupported operator → ERR-PLN-002 copy inline" | **FAIL** (tree editing CONFIRMED; negative path fabricated and cites the wrong error) | Tree edit handlers confirmed real: `CriteriaBuilder.tsx:61-71` (`addCond`/`addGroup`/`removeAt`/`patchCond`/`move`), keyboard-operable move buttons exercised by the live "keyboard-operable" Playwright test. **But** `grep -n "ERR-PLN" CriteriaBuilder.tsx page.tsx BulkForm.tsx` → 0 matches — no such copy exists anywhere. The operator `<select>` (`CriteriaBuilder.tsx:85-88`) only ever offers `is`/`is-not`, so an "unsupported operator" state is not even reachable from the UI. Separately, `product-contract/governance/error_catalogue.csv` defines `ERR-PLN-002` as **"No target factories / Valid search returns none"**, not an operator-validation error — the row cites an error ID that does not match its own catalogued scenario. |
| 4 | Save criteria / draft | `HANDOFF_BLOCKED` — draft persistence not implemented (actions.ts only writes criteria at publish) | **BLOCKED_UPSTREAM** (accurately reflects the code) | Confirmed no draft-save code path exists anywhere in `page.tsx`/`actions.ts`; the only `visit_plans` insert happens inside `publish_bulk_plan` at actual publish time. Correctly left unbuilt rather than inventing a persistence mechanism. |
| 5 | Restore draft | `HANDOFF_BLOCKED` — same dependency as row 4 | **BLOCKED_UPSTREAM** (accurate) | Same evidence as row 4 — `page.tsx` never queries `visit_plans` for an existing draft on GET render. |
| 6 | Select row / select visible / select all results | Dup rows disabled (M02-012); negative: "Select-all requires typed count confirm" | **FAIL** (dup-disable CONFIRMED; typed-confirm claim fabricated) | `BulkForm.tsx:67-68` (`selectVisible`/`selectAllResults` both skip `dupOf(f)` rows) and `:122` (checkbox `disabled={dup}`) — real. **But** `selectAllResults` (`BulkForm.tsx:68`) is wired directly to a plain button `onClick` (`:105`) with **no confirmation dialog, no typed-count input, no intermediate step of any kind** — selecting "all results" (which can span many pages) happens on a single click. |
| 7 | Open factory profile | `target=_blank`; RLS read; automated_test: "existing factories tests" | **FAIL** (behavior itself correct; test claim fabricated) | `BulkForm.tsx:123` — `<a href={`/factories/${f.id}`} target="_blank" rel="noreferrer">` — genuinely correct and RLS-scoped (`factories_read` policy). But `find apps/web/e2e -iname "*factor*"` → **no matching file at all** in the e2e directory. The row asserts an existing test suite covers this; no such file exists anywhere in the repo. |
| 8 | Review & continue | client_component BulkForm; validation_guard "Readiness chips gate"; negative "Blocked reasons attached to button"; runtime_evidence "DESIGN-NEW (today publish is same-page; relocation pending approval)" | **FAIL** (stale claim; and the relocation it calls "pending approval" already shipped without the approval its own manifest required) | The claim that "today publish is same-page" is false against the current runtime: `BulkForm.tsx:170` already links to `/planning/bulk/review`, and `review/page.tsx` + `review/ReviewClient.tsx` are fully built (confirmed live via Playwright: "P02 review step configures, assigns and exposes atomic publish" passes). The gate is not "readiness chips" — it is a single selection-count check (`BulkForm.tsx:171`, disabled only when `selected.size === 0`); no chip UI for package/inspector/validation readiness exists on this screen (those moved to the review step). No "blocked reasons attached to button" exists — only a generic `⚠ select at least one factory` lozenge elsewhere in the bar. **Governance concern**: `outputs/cd-021/IMPLEMENTATION_MANIFEST_CD-021.yaml:38` explicitly states this relocation of visit-type/package/window/notes/inspector fields to the P02 step "requires human approval as an IA change." No such approval exists — `grep -n -i "cd-021" product-contract/governance/HUMAN_APPROVALS.yaml` returns nothing, and the manifest's own top-level `status:` field (line 59) still reads `READY_FOR_DESIGN_REVIEW`, never `APPROVED`. Yet the relocation is fully built and committed (`ea22b20`, commit message literally says "P02 review route relocation"). This is exactly the kind of undisclosed structural change the checklist's cross-cutting "nothing in removals was actually removed unless approved" check exists to catch — the manifest's own `removals: []` field is technically correct (nothing was deleted) but the same-page publish flow described by CD-021's own design frames was replaced without the required sign-off. |
| 9 | Publish plan | Blockers list; visits/plan draft→published; RLS all tables; audit rows; notifications insert; negative "PARTIAL FAILURE: step-level truth... atomicity HANDOFF_BLOCKED (no txn/RPC in actions.ts)" | **PASS** (substance verified live; the row's own text is stale but describes a worse — now-fixed — state, not a hidden defect) | Blocker list matches verbatim: `actions.ts:66-110` (empty selection / `ERR-PUB-001` package / `FLD-PLAN-005` window / M02-012 dup with skip / inspector pool / M01-029 conflicts). `actions.ts:121-130` calls `sb.rpc("publish_bulk_plan", ...)`; RLS confirmed via `visit_plans`/`visits`/`assignments` policies (`0002_rbac_audit.sql:20-27`) and `security invoker` in the function definition. Audit and notifications confirmed automatic (see "Independent re-verification" above). Neutral copy confirmed: `actions.ts:131-135` logs `error.message`/`error.code` to `console.error` only, returns the single catalogued `NEUTRAL_PUBLISH_ERROR` string to the client — no raw provider text reaches the DOM. Atomicity independently proven live (forced-FK rollback test above) — the row's own claim that atomicity is "HANDOFF_BLOCKED (no txn/RPC in actions.ts)" is **false today**; migration 0026 fixed exactly this gap the same day the CSV was last read. There is also no "step-level truth" partial-failure ledger UI in `ReviewClient.tsx` (`state.error` is a flat blocker/error string list, not a per-step status display) — the CSV describes an older, non-atomic architecture's UI that no longer exists. Recording this row PASS reflects that the *requirement* (P03 atomic, non-partial publish, neutral errors, real audit) is genuinely met; the CSV's own descriptive text needs to be rewritten, not treated as evidence. |
| 10 | Retry publish | `HANDOFF_BLOCKED` — resumable variant not in current runtime | **BLOCKED_UPSTREAM** (accurate) | Confirmed: `actions.ts` exports a single `publishBulkPlan` with no `resumeId`/resumable parameter anywhere, and `ReviewClient.tsx` has no retry-specific code path. Correctly left unbuilt rather than fabricated. (Note: because the underlying RPC is now fully atomic, a naive resubmit-after-failure is not unsafe — a genuinely failed call leaves zero residue to collide with — but true resumability, e.g. reusing a partially-notified plan, remains unimplemented, exactly as this row states.) |
| 11 | Source freshness / stale banner | `HANDOFF_BLOCKED` — "no sync-timestamp source exposed in current bulk page; requires governed field" | **PASS** (feature genuinely shipped since the CSV was last read; correctly does not invent a threshold) | This claim is also stale: `page.tsx:69-71` reads `factories.source_synced_at` (a real, pre-existing column per `0001_foundation.sql:134`) over the matched set, computes `oldestSyncedAt`/`missingSync`, and `EligibilityLedger.tsx:32-34,56-63` renders the factual timestamp/missing-count — with **no invented "stale" threshold**, consistent with the project's "never invent thresholds" rule (no governed staleness SLA exists in `engine_settings`). The behavior is correct; the CSV cell is out of date. |
| 12 | Partial source failure (risk read) | DistributionPanels isolates failure per source (`ERR-OPS-001`); monitoring event; "Other panels usable; risk cells 'unavailable'"; "Never a guessed band; publish blocked with reason" | **FAIL** (no per-source isolation exists — there is only one, unhandled, shared data source) | `page.tsx:30-33` fetches region/risk_band/activity_class/etc. from a **single** `factories` query with no `error` destructured (`const { data: allFactories } = await sb.from("factories")...`) — there are no independent "sources" to isolate per panel; region/risk/activity distributions are all derived from the same one query's result in-memory (`page.tsx:49-65`). If that single query fails, `allFactories` becomes `[]`, and the ledger/table/distributions all silently render a false "0 in scope, 0 eligible" state — not the claimed per-panel "unavailable" messaging, no monitoring event insert anywhere in the file, and no distinction from a genuinely empty catalog. This is the same class of defect flagged in the CD-022 audit's Finding 1 (an unhandled query error masquerading as a legitimate empty result), here affecting the entire screen rather than one search. |
| 13 | Unauthorized access | Role check + RLS empty scope; "RLS denies"; "State-17 no-data screen"; "No client-side bypass" | **FAIL** (no role guard exists; RLS is deliberately wide, not empty, for reads) | `grep -n "user_roles" page.tsx` → 0 matches — `page.tsx` never queries or branches on the caller's own role. `factories_read` policy (`0002_rbac_audit.sql:15`) is `using (auth.uid() is not null)` — open to **any authenticated user**, not role-scoped. `Shell.tsx:19` queries `user_roles` only to build navigation groups (`buildShellNavigation`), which is a UI convenience, not an authorization boundary (consistent with the same finding already independently confirmed for the sibling CD-022 screen). A non-planner authenticated user (inspector/reviewer/ops) hitting `/planning/bulk` or `/planning/bulk/review` directly gets the **full functional targeting/review UI** — criteria builder, ledger, distributions, evidence table, visit configuration, inspector assignment — not a "State-17 no-data screen." Only the final `publish_bulk_plan` write would fail (RLS `has_role('planner')` on `visit_plans`/`visits` insert), surfacing the generic neutral publish error, not a distinct unauthorized/no-data screen as the row claims. |

## FAIL findings

### FINDING 1 — Fabricated "invalid condition" negative path (row 1)
- verdict: CONFIRMED
- evidence: `apps/web/src/app/planning/bulk/CriteriaBuilder.tsx` (no `alert` role anywhere); `apps/web/src/app/planning/bulk/criteria.ts:46-64` (`fromWire` silently drops anything malformed, returns `null`); `page.tsx:24-26` (`parseCt(...) ?? fromFlat(...) ?? emptyTree()` — falls all the way through to "match everything," never an error state).
- check: request `/planning/bulk?ct=not-json` or `?ct={"k":"bogus"}` and confirm the UI shows anything other than the normal unfiltered results — it does not; no alert, no "frozen" state.
- required correction: either implement a real invalid-criteria alert state, or correct the wiring-map row to state this negative path does not exist (mark `HANDOFF_BLOCKED` with a decision, not an unverified claim).

### FINDING 2 — "Focus condition" feature has zero runtime implementation (row 2)
- verdict: CONFIRMED
- evidence: no "focus"/highlight logic anywhere in `CriteriaBuilder.tsx`, `BulkForm.tsx`, `EligibilityLedger.tsx`, or `DistributionPanels.tsx`; the reserved `focusContribution` string (`page.tsx:81`) is declared but never rendered.
- check: click any condition row looking for a contribution highlight in the ledger/distribution/table — no such interaction exists anywhere in the DOM or the client code.
- required correction: implement the feature, or formally record it as deferred (decision ID) rather than leaving a fully-specified but unbuilt claim standing.

### FINDING 3 — Fabricated "unsupported operator" negative path citing the wrong error ID (row 3)
- verdict: CONFIRMED
- evidence: `CriteriaBuilder.tsx:85-88` (operator `<select>` only ever offers `is`/`is-not` — the state is unreachable from the UI); no `ERR-PLN-002` text anywhere in `apps/web/src/app/planning/bulk/`; `product-contract/governance/error_catalogue.csv` row `ERR-PLN-002` = "No target factories / Valid search returns none," not an operator-validation error.
- check: `grep -n "ERR-PLN" apps/web/src/app/planning/bulk/*.tsx` — no matches.
- required correction: remove this negative-path claim (the state is not reachable) or correct the row to cite the actual governed error for unsupported/invalid criteria, if one is created.

### FINDING 4 — "Select all results" has no typed-count confirmation (row 6)
- verdict: CONFIRMED
- evidence: `BulkForm.tsx:68` (`selectAllResults`) is bound directly to a button `onClick` (`:105`) with no intervening dialog or input.
- check: click "Select all results" with a large filtered set — selection completes immediately, no count-confirmation step of any kind.
- required correction: implement the typed-count confirmation the row claims, or remove the claim from the wiring map.

### FINDING 5 — "Existing factories tests" do not exist (row 7)
- verdict: CONFIRMED
- evidence: `find apps/web/e2e -iname "*factor*"` returns no files.
- check: same command, repeatable.
- required correction: correct the `automated_test` cell to `proposed` (an honest gap) rather than citing a non-existent test suite. The underlying `target=_blank` behavior itself is correct and does not need code changes.

### FINDING 6 — P02 relocation shipped without the human approval its own manifest required (row 8)
- verdict: CONFIRMED
- evidence: `outputs/cd-021/IMPLEMENTATION_MANIFEST_CD-021.yaml:38` ("relocation requires human approval as an IA change"); manifest `status:` (line 59) still `READY_FOR_DESIGN_REVIEW`; `product-contract/governance/HUMAN_APPROVALS.yaml` has no CD-021 entry; commit `ea22b20`'s own message states "P02 review route relocation" as delivered.
- check: `grep -in "cd-021" product-contract/governance/HUMAN_APPROVALS.yaml` — no output.
- required correction: obtain and record the sponsor approval this IA change requires (or revert the relocation), and update the wiring-map row and manifest status to reflect whichever outcome is chosen — the row cannot keep describing this as "pending approval" when it has already shipped and been committed.

### FINDING 7 — No per-source failure isolation for distributions/ledger; single unhandled query masks a real outage as an empty result (row 12)
- verdict: CONFIRMED
- evidence: `page.tsx:30-33` — `const { data: allFactories } = await sb.from("factories")...` discards any `error`; every downstream panel (`EligibilityLedger`, `DistributionPanels`, `BulkForm`'s table) derives from this one in-memory result with no independent per-panel data source to isolate.
- check: force the `factories` query to fail (e.g. transient outage) and observe the UI — it shows "0 in scope / 0 eligible / empty distributions," indistinguishable from a legitimately empty catalog; no monitoring event, no "unavailable" messaging as `ERR-OPS-001` requires.
- required correction: check the query's `error` and render the catalogued `ERR-OPS-001` unavailable state (with a monitoring event) instead of silently treating a fetch failure as an empty result — or correct the wiring-map row to state this isolation does not exist, since the architecture (one shared query) doesn't currently support per-source isolation at all.

### FINDING 8 — No server-side role guard on `/planning/bulk` or `/planning/bulk/review`; wiring row's claimed behavior is false (row 13)
- verdict: CONFIRMED
- evidence: `apps/web/src/app/planning/bulk/page.tsx` (no `user_roles` query at all); `apps/web/src/components/Shell.tsx:19` (role query used only for nav, not gating); `supabase/migrations/0002_rbac_audit.sql:15` (`factories_read` — any authenticated user).
- check: sign in as a non-planner role and navigate directly to `/planning/bulk` — the full targeting UI renders; only the final publish write would fail via RLS, not a distinct "no-data state 24/17" screen.
- required correction: add an explicit server-side role check (mirroring the pattern already used in `apps/web/src/app/planning/immediate/page.tsx`), or correct the row to describe the actual (RLS-write-blocked-but-UI-visible) behavior. This is the same class of gap independently confirmed in the CD-022 post-implementation audit for the sibling `/planning/single` screen — it is not new to CD-021, but CD-021's wiring map still asserts a protection that does not exist.

## Checks that passed (not exhaustive — see per-row table above)

- Typecheck, production build (`/planning/bulk` + `/planning/bulk/review`), color-law scan: all clean.
- Live Playwright: 15/15 pass (3 auth-setup + 12 in `cd-021-bulk-targeting.spec.ts`) on a freshly started production build, real Supabase project — confirmed read-only (no test clicks "Publish").
- **Atomicity independently proven live**, not read from a comment: a forced FK-violation call to `publish_bulk_plan` left zero orphan `visit_plans` rows (8 before, 8 after) — migration 0026 genuinely fixes the non-transactional-6-write defect the wiring map itself documents.
- RLS is the real authorization boundary for all writes: `visit_plans`/`visits` insert require `has_role('planner')`, `assignments` insert requires planner-or-ops, all via `security invoker` (no service-role bypass anywhere in `actions.ts` or migration 0026).
- Audit trail is automatic and append-only: `trg_audit_visit_plans`/`visits`/`assignments` fire inside the same transaction as the RPC's writes; `audit_events` has `update`/`delete` revoked from `public`.
- No raw provider/DB error text reaches the client: `actions.ts:131-135` logs the real Supabase error server-side only, returns one catalogued neutral string.
- Notifications are honestly `delivery_state default 'queued'` — no fabricated "delivered" language.
- Nested ALL/ANY criteria evaluation, legacy `cf`/`co`/`cv` backward-compatibility, defensive depth/node caps (`criteria.ts:20-21`), and the shared parse/serialize/eval module used by both server and client are real and exercised live.
- Selection persistence across pagination/filtering and the selection-invalidation confirm dialog (never silent) are real, `sessionStorage`-backed, and exercised live.
- M02-012 duplicate flags and M01-029 inspector-pool/overlap checks are enforced server-side in `actions.ts`, not merely client-decorative.
- Non-color-only status (FND-011) is real: every eligibility/duplicate/data-quality/double-booking lozenge pairs a glyph (`✓ ⛔ ⚠ − ?`) with text, confirmed both by code read and the live Playwright glyph assertion.
- Freshness (FND-013) genuinely uses the real `factories.source_synced_at` column and does not invent a staleness threshold.

## Next allowed action

Per DEC-012 and the checklist's verdict rollup, this slice returns to implementation, not closure, on the 8 FAIL rows above (Findings 1–8). None requires new product policy to fix — they are documentation/code gaps: (1)-(3) either build the claimed negative paths or correct the wiring map to remove unverified claims; (4) either build the typed-count confirmation or drop the claim; (5) correct the `automated_test` cell to `proposed`; (6) obtain and record the sponsor approval the manifest itself requires for the P02 relocation (or revert it) before this slice can be considered structurally sound; (7) handle the `factories` query's `error` path per `ERR-OPS-001` or correct the row; (8) add the same server-side role guard already used by `planning/immediate/page.tsx`, or explicitly accept the gap via a recorded decision. Rows 4, 5, and 10 (`BLOCKED_UPSTREAM`) and rows 9 and 11 (`PASS`) do not block closure on their own, but the wiring map's stale descriptive text for rows 9 and 11 should still be corrected so the document reflects reality. After remediation (or explicit accepted-gap decisions), a fresh reviewer pass over just the changed rows — plus the still-outstanding sponsor runtime acceptance — would be the remaining path to closing CD-021.
