# Module 06 — Bulk Criteria, Search and Selection

Expand the typed criteria model to every documented field and operator with nested AND/OR groups. Require a criterion unless unrestricted permission. Use canonical query/projection with pagination and honest unavailable fields. Implement page/all-matching selection with configured maximum, Factory360 detail, persisted server draft, selected-count continuity, and per-row eligibility reasons. Never load/provider-call per row from browser.
