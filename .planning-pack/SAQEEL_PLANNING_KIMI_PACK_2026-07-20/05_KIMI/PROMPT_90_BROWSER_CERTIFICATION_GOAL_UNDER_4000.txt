GOAL: Independently certify the completed SAQEEL Planning branch in Chrome and fix every engineering-controlled defect in the same loop. Read `PROMPT_90_BROWSER_CERTIFICATION_BRIEF.md`, the canonical plan, all ledgers/source documents, and the complete final diff before opening the browser. Run the app from the planning worktree at `http://127.0.0.1:3000` against the approved staging backend; confirm PID, cwd, branch and served SHA. Do not use a hosted URL or another port.

Execute every row in `PLANNING_ACCEPTANCE_AND_BROWSER_JOURNEYS.csv` and trace every Planning requirement to a visible result, server/RLS result, audit record or test. Cover Admin, Inspector, default business staff and unauthorized/anonymous; list/tabs/columns/search/filters/export; all entry points; persisted drafts; Single CR/licence/plant/multiple licences; manual eligibility; Bulk fields/operators/nested AND/OR/select-all/eligible-ineligible; assignment/distribution/conflicts; maps/location provenance; optional and multiple packages; publish fan-out; inspector pool; return/republish; cancellation cut-offs; all expiry rules; duplicate; details; notifications/audit; dashboard count reconciliation; provider failure isolation; Arabic/RTL, dark/light, desktop/iPad-width, keyboard/focus and non-colour status. Verify no direct provider client, credential, invented contract or false zero.

Do not stop at the first failure or return checkpoints. For each defect: reproduce, record, root-cause in code/schema/RLS/config/data, fix, rerun the journey, run affected regression, continue. Create minimal `PLN Runtime` fixtures only when needed; use existing workflows, list all IDs, and clean or govern retained immutable fixtures. Run typecheck, production build, Planning contracts and full protected regression, diff-check and delta secret scan. Push the repaired branch; do not merge/deploy. Return one final report with journey statuses, defects/fixes, test totals, evidence paths, final SHA and genuine external blockers only. End: AWAITING_SPONSOR_PLANNING_BROWSER_ACCEPTANCE
