# API Source Safety Note

The uploaded raw `Inspection_API_documentation(2).json` contains example passwords, bearer tokens and personal data. It is intentionally **not included** in this redistribution pack. The sanitized structural equivalent from `API Docs Set 2.zip` is included instead. Kimi must use it for schemas/field paths only and must never reproduce or commit raw values.

Original Planning.docx, consolidated BRD PDF, API Set 2 ZIP and both supplied images are included unchanged.
