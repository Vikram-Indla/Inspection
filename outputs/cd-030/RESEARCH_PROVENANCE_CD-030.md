# RESEARCH_PROVENANCE_CD-030.md — R1
DO NOT EXECUTE UNTIL SPONSOR DESIGN APPROVAL AND INDEPENDENT CODEX WIRING AUDIT

Non-product. Three primary sources; observed / adopted / rejected. Informs information architecture and integrity treatments only — no runtime behaviour inferred.

## 1. Regulated document version-comparison / redline pattern
- Observed: a trustworthy comparison separates authorized edits from out-of-scope edits and never implies equality it did not verify.
- Adopted: the Tamper-evident Scope Rail classifies changes against the stored returned scope; explicit 'unavailable' categories; navigation-only.
- Rejected: a code-diff red/green aesthetic and auto-merge/accept-changes — unsafe for immutable regulatory versions and unproven in the runtime.

## 2. Saudi public-service bilingual transaction pattern
- Observed: full-document RTL, mixed-direction identifiers/dates via bidi isolation, immutable outcome provenance.
- Adopted: bdi isolation for version numbers, factory/visit codes and timestamps; an immutable version/audit provenance panel.
- Rejected: colour-only change signalling; English-only codes embedded in an Arabic layout.

## 3. Accessibility / RTL authoring guidance (disclosure + non-color status)
- Observed: native disclosure controls, list-equivalent structure, non-color status, reduced-motion equivalents.
- Adopted: scope-rail categories as button disclosures with glyphs; semantic comparison table; role=status/alert; reduced-motion static equivalent.
- Rejected: hover-only diff reveal; an SVG diff graph with no list fallback.

## Baseline
BASELINE_REVERIFY_REQUIRED — the /reviews/:id version-diff sources were read at main this session. No exact-baseline equivalence is claimed; exact code/wiring verification is deferred to independent Codex audit.
