# CANDIDATE ARCHITECTURES + DECISION MATRICES — CD-012→019 R2

Three genuinely distinct architectures per screen (IA + task flow, not styling). SELECTED is the
built one; the harness renders it at full fidelity. Rejected options are recorded with rationale;
their full-fidelity rendering was not produced in this environment — declared in PACKAGE_PREFLIGHT.

## CD-012 — A) maker-checker library + version lineage (SELECTED: one-scan answer to "what's live, who's accountable, what's in flight") · B) approval-queue board (rejected: buries superseded lineage; weak cross-library comparison) · C) per-workflow version timeline (rejected: buries library-wide view; belongs to the editor screen)
## CD-013 — A) payload editor + read-only structure + quarantined designer lane (SELECTED: present truth first; ambition visible but non-executable) · B) canvas-first designer (rejected: shows unbuilt capability as a tool — the R1 error) · C) form-per-transition wizard (rejected: destroys whole-model reading; slower than JSON for admins)
## CD-014 — A) trace-first reproducibility workspace over engine_settings (SELECTED: the score explains itself from stored facts) · B) KPI/gauge dashboard (rejected: gauge theatre; numbers without arithmetic) · C) draft/approve version workflow (rejected: invents a lifecycle the backend does not have — the R1 error)
## CD-015 — A) coordinates-provenance + radius editor, map as preview (SELECTED: facts work without tiles; the one real write is prominent) · B) map-led studio (rejected: implies spatial authority no provider supports) · C) bulk geometry table (rejected: buries the single-factory decision; no provenance)
## CD-016 — A) blocked concept boundary + prerequisites (SELECTED: the only honest primary state) · B) functioning rule studio (rejected: every control would be a lie — the R1 error) · C) hide the destination entirely (rejected: loses the hand-off value; sponsor needs the prerequisites enumerated)
## CD-017 — A) read-only holdings + non-conclusive explainer + change boundaries (SELECTED) · B) editable capability matrix with approve flow (rejected: no change workflow exists — the R1 error) · C) per-user access page (rejected: hides cross-user role patterns; scope column lost)
## CD-018 — A) working rows with real actions + history/restore-as-draft (SELECTED: core actions exist; priority slice) · B) completion-percentage dashboard (rejected: quality ≠ completion; hides context) · C) screen-context walker (rejected: slow for bulk review; context capture is a manual step today)
## CD-019 — A) reader-first: filters + pagination + verbatim diff (SELECTED: matches the real reader exactly) · B) correlation timeline hub (rejected as primary: no correlation column exists; kept as a labelled proposal) · C) analytics/totals overview (rejected: the reader computes no totals — the R1 error)

Decision criterion throughout: the 30-second question each persona actually asks, answered using
only present-truth reads/writes; ambition quarantined, never executable.