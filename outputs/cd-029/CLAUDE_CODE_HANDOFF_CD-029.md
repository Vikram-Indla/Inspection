# CLAUDE_CODE_HANDOFF_CD-029.md — R1
DO NOT EXECUTE UNTIL SPONSOR DESIGN APPROVAL AND INDEPENDENT CODEX WIRING AUDIT
implementation_authorized: false

## Package
Single synchronized package at outputs/cd-029-r1/ : editable source (CD-029 Level 2 Review Workspace.dc.html), standalone, cd29-stage.js, cd29-annot.js, saqeel-tokens.css, saqeel-retired-predecessor.css, saqeel-prism.svg, manifest, component/wiring/state maps, acceptance checklist, research provenance, this handoff, the implementation prompt, inventory, and evidence PNGs.

## Signature (binding)
Finding Trace Chain: question → response → evidence → clause → violation → corrective action → decision comment. Keyboard-operable disclosure list, source/version-labelled, list-equivalent, never a decorative graph; unavailable links shown, never fabricated.

## Binding HANDOFF_BLOCKED (design surfaces; does not resolve)
- HANDOFF_BLOCKED_PAGELOAD_MUTATION — opening /reviews/:id on a submitted inspection with no open review inserts a review and moves the inspection to under_review on load. Surface it honestly; making open side-effect-free is a backend workflow decision, not done here.
- HANDOFF_BLOCKED_ATOMIC — the decision write is review update → inspection update → notification insert, non-transactional; a partial commit is possible. Never claim atomic completion, rollback or delivery.
- HANDOFF_BLOCKED_MEDIA — no provider-backed media viewer is proven; evidence preview is explicitly unavailable.
- HANDOFF_BLOCKED_CLAIM — no claim/reassign path; shown unavailable.
- HANDOFF_BLOCKED_LINKED — clause/violation mapping and factory-verification sources can fail; affected links read 'unavailable'.
- HANDOFF_BLOCKED_ERRORMAP — 'contact support' copy has no governed destination.

## Preserve
Read-only immutable version (edits DB-rejected); return = exact sections + reason; reject = reason; diff from stored answers; append-only audit; decided reviews DB-locked; factory verification never mutates the Senaei source (0020 pending shown verbatim); notifications queued-not-delivered; the grouped shell. CD-028 queue is not redesigned.

## Baseline
BASELINE_REVERIFY_REQUIRED — /reviews/:id sources read at main this session; re-verify locally before editing. setup/Inspection is obsolete.
