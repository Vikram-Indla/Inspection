prompt_id: CD-023
screen_id: SCR-WEB-130
route: /planning/immediate
personas: [Planner, "specifically authorized Inspector (NO runtime mechanism - HANDOFF_BLOCKED)"]
journey: P01
engines: [ENG-03, ENG-05, ENG-06, ENG-12]
acceptance: [DSG-018, DSG-A11Y-001]
source_branch: feat/cd-003-role-resolution   # per CURRENT_STATE.md; local mount exposes no git metadata
source_commit: UNVERIFIED_HANDOFF_BLOCKED
dirty_worktree: UNVERIFIED_HANDOFF_BLOCKED
earlier_planning_designs: HANDOFF_BLOCKED    # CD-020/021/022 have no recorded sponsor approval; only the frozen shared shell is assumed
research_set_r16_r21_r24_r25: HANDOFF_BLOCKED # approval-pack Reference Library defines R01-R15 only; cited IDs undefined in the mounted repo; categories satisfied directly
design_frame_ids: [3a, 3b, 3c, 3d, 3e, 3f, 3g, 3h, 3i]
design_file: "CD-021 Bulk Targeting.dc.html (canvas section CD-023, ids 3a-3i)"
png_paths:
  - outputs/cd-023/CD-023_SCR-WEB-130_primary.png
  - outputs/cd-023/CD-023_SCR-WEB-130_unregistered.png
  - outputs/cd-023/CD-023_SCR-WEB-130_unauthorized.png
  - outputs/cd-023/CD-023_SCR-WEB-130_partial_failure.png
  - outputs/cd-023/CD-023_SCR-WEB-130_ar_rtl.png
  - outputs/cd-023/CD-023_SCR-WEB-130_narrow.png
selected_composition: Compact authority-bar flow (3a); consequence-first review (3b) and context-first identity/location (3c) rejected with rationale on canvas
confirmed_runtime_defects:
  - "actions.ts hard-codes region:'Riyadh' for temporary factories; captures no city (prompt risk 1+2 CONFIRMED)"
  - "assignments insert error is UNCHECKED - flow notifies and redirects even when assignment failed (risk 3 CONFIRMED)"
  - "4 sequential writes (temp factory, visit, assignment, notification), no transaction; orphan temp factory possible (risk 4 CONFIRMED)"
  - "no duplicate/retry protection; visit born planning_status:'published' with no M02-012 check (risk 5 CONFIRMED)"
  - "raw e.message returned to UI"
  - "ImmediateForm uses uncontrolled inputs WITHOUT the resetKey pattern - React 19 reset wipes entered work on blocked create"
  - "lat/lng inputs prefill Riyadh center 24.7136/46.6753 - dangerous default"
  - "priority is ungoverned free text (no approved value list exists in the contract)"
file_changes:
  - file: apps/web/src/app/planning/immediate/page.tsx
    before: Fetches non-temporary factories + packages + inspector roles; builds strings; renders ImmediateForm
    disposition: UPDATE
    after: Adds region/city option lists for the temporary path (distinct values from factories - no new source invented); passes registry freshness if a governed sync field exists (else omitted)
    design_node: 3d region/city selects
    protected: RLS-scoped reads; is_temporary=false filter for registered search
    tests: temporary path requires region+city
  - file: apps/web/src/app/planning/immediate/ImmediateForm.tsx
    before: Two-column form; CR/license/name search over full list; manual capture fields; free-window; priority free text; uncontrolled inputs; single error list
    disposition: UPDATE
    after: Authority-bar composition - persistent 9-protection bar (focusable chips, jump-to-control); registered/unregistered explicit toggle; registered identity preview card; required region+city on temporary path; NO coordinate prefill; consequence summary beside dispatch; controlled inputs + resetKey work preservation; narrow 9-dot strip
    design_node: 3a/3d/3i
    protected: governed urgency enum (3 values); M01-044..048 gates; auto-assign default; urgent default window M01-047
    tests: keyboard chip navigation; work preservation on blocked create; no-prefill; region/city required
  - file: apps/web/src/app/planning/immediate/actions.ts
    before: createImmediateVisit - blockers, availability check, then temp-factory insert (region hardcoded Riyadh), visit insert (published, no plan), UNCHECKED assignment insert, notification, redirect; raw e.message
    disposition: UPDATE
    after: (a) region+city from form on temporary path (no hardcode); (b) CHECK assignment insert result - failure returns structured step state instead of continuing; (c) structured step results for the 3f ledger; (d) retry keyed to created ids (temp factory + visit) - never duplicates; (e) catalogued neutral error copy; (f) refuse temp-entity creation when an EXACT registered CR match exists
    design_node: 3f reconciliation; 3d
    protected: canonical states; append-only audit; notification insert semantics (queued); candidates JSON audit
    dependencies: HANDOFF_BLOCKED - atomicity/compensation needs a Postgres RPC (backend decision); temp-entity reconciliation queue surface does not exist (governed gap); inspector immediate-authorization grant does not exist (do not invent)
    tests: forced failure at each write; retry idempotence; orphan accounting; no raw provider text
  - file: apps/web/src/app/planning/immediate/AuthorityBar.tsx
    before: (does not exist)
    disposition: CREATE
    after: 9-protection bar deriving entirely from existing validation state (no new policy objects); chip states satisfied/blocking/truth-labelled; roving tabindex; assertive announcements; narrow compression
    design_node: 3a/3g/3h/3i
    tests: chip focus/jump; announcement assertions; all-states rendering
removals: []   # REMOVE requires explicit human approval; none proposed
priority_field: kept as optional ungoverned free text, truth-labelled in UI; approved-value enum is HANDOFF_BLOCKED (no governed list exists - do not invent)
rtl_theme_impact: Logical properties; bar chips mirror; bdi-isolated CRs/coords/times; dark/light via ax tokens; amber reserved for degraded truths - no red emergency theatre
rollback: Additive UPDATEs on the same route; git revert; no schema changes proposed (region/city use existing factories columns)
status: READY_FOR_DESIGN_REVIEW
