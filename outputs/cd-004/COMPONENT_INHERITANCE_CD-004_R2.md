# COMPONENT_INHERITANCE_CD-004_R2.md
FREEZE (untouched): Shell.tsx, ShellClient.tsx, shell-navigation.ts, NotificationBell.tsx (never engine-health evidence), tokens.css.
PRESERVE (consumed): retired-predecessor grammar — legacy-surface, legacy-table/tablewrap, legacy-lozenge (glyph+word), legacy-version (mono), legacy-freshness (fact only), banner/state; i18n runtime (keys enumerated in LOCALIZATION_INVENTORY, no hard-coded production copy); /admin/audit as link target only.
REFINE_IN_DESIGN (proposed, post-approval): admin/page.tsx composition — KPI wall -> evidence spine; static live-database lozenge -> per-read source-state lozenge; page-local Promise.all -> per-source result modelling.
DESIGN_HISTORY_ONLY: design/retired-predecessor/d2/** — vocabulary consulted; health verdicts/thresholds/approval counts rejected.
HANDOFF_BLOCKED: admin-family route guard; per-destination read-only vs deny behavior; proposed provenance/draft-queue reads (see DATA_TRUTH_LEDGER).
Single signature pattern: Configuration Evidence Spine (deletion-tested, frame r2k). Nothing else is new.
