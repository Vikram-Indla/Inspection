# Claude Design — CD-031 R2 Micro-correction Prompt

Paste this complete prompt into Claude Design. Correct only these two CD‑031 R2 acceptance failures. Do not redesign or implement application code.

`implementation_authorized: false`

Every Claude Code-facing file starts exactly:

`DO NOT EXECUTE UNTIL SPONSOR DESIGN APPROVAL AND INDEPENDENT CODEX WIRING AUDIT`

1. Make every interactive Factory 360 section-navigation pill at least **48×48px** in desktop, Arabic RTL and 412px narrow layouts. Preserve the repaired separated labels, boundaries, selected state and visible keyboard focus.
2. In `PACKAGE_PREFLIGHT_CD-031.md`, record the actual SHA-256 value for each complete A/B/C hypothesis PNG and confirm all three values differ. “Distinct bytes” is not sufficient.

Preserve the clean single-root archive, all R2 timeline truth blocks, all existing states, all routes/roles/blocked classifications, and the frozen shared shell.

Deliver only `outputs/cd-031-r3/` and archive only that directory. All governed files must identify CD-031, SCR-WEB-400 and R3. Return `PACKAGE_PREFLIGHT_PASS` and `READY_FOR_DESIGN_REVIEW_R3` only when the target-size proof and literal SHA-256 values are present. Do not implement.
