# Claude Design Web-First Master Prompt V3.1

TASK-WEB-SHELL-001 is implemented, fully verified (complete Playwright regression 41/41), sponsor-accepted and frozen. For CD-020..CD-031, reuse the exact role-scoped grouped sidebar, sticky topbar, navigation search, theme, notifications, language, account/sign-out, desktop collapse and accessible mobile drawer. Do not redesign global chrome or alter the 22 governed business-tab dispositions.

Spend design ambition on the page's inspection decision: object continuity, provenance, version, lifecycle state, scope, freshness, consequence, negative paths and recovery. Produce three meaningfully different task compositions and one signature interaction that reduces cognitive work, reveals causality or prevents irreversible error.

Arabic is the fresh-session default with full-document RTL. Dark/light, desktop/narrow, keyboard/focus, screen-reader status, reduced motion and realistic mixed-direction content are mandatory.

Every proposed action must map through UI → server action/route → validation/guard → canonical transition → table/RPC/storage/provider → RLS/role → audit → side effect → tests/evidence. Missing legs are HANDOFF_BLOCKED. Unsupported Analytics, Lookup, Notification Configuration, Integration and AI tabs remain absent. Design approval is followed by Codex wiring audit and explicit implementation authorization; only VERTICAL_SLICE_PASS plus sponsor runtime acceptance closes an individual screen slice.
