# External Claude Design Approval Pack

The verified approval-pack export, transfer archives, renders, and duplicate snapshots are stored at `INSPECTION_DOCS_ROOT/05_UI_UX_AND_STORYBOARDS/outputs/claude-design-approval-pack`.

Small live wiring maps and acceptance artifacts outside this packaged export remain in `outputs/**` where repository validators require them. See the external `MANIFESTS/move_plan.csv` for exact file locations and SHA-256 values.
