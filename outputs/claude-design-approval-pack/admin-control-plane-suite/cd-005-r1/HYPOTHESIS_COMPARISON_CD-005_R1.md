# HYPOTHESIS_COMPARISON_CD-005_R1.md
Equal fidelity, identical dataset (5 regulations), identical hard case (one mapped-item read failed + one clause-less draft), Arabic-first. Frames: A=5e-5j, B=5b, C=5c.

A — Lifecycle-led legal register (SELECTED): dense list, Impact Footprint Rail attached to the active row.
B — Authority/provenance navigator: issuing-authority hierarchy → regulations → clauses.
C — Exception-first governance queue: problems first, full library secondary.

| Criterion | A | B | C |
|---|---|---|---|
| Decision speed | one scan per row (lifecycle+footprint+freshness co-located) | two drill levels | fast only with a real exception engine (none proven) |
| Provenance clarity | full legal-source chain for the active row, at the point of decision | strongest globally, but decoupled from lifecycle | minimal |
| Unknown vs zero safety | bound to the object (rail attached to the failed row) | equal grammar, detached | equal grammar, detached |
| Irreversible-action prevention | publish removed from library; rail argues against careless publish downstream | same | same |
| Arabic RTL clarity | single-column register reads naturally RTL | three-pane cascade fights RTL scan order | fine |
| Keyboard burden / 1024 survival | one linear list; rail stacks full-width under row at 1024 | pane-hopping, worst at 1024 | two modes to learn |

Selection: A, keeping B's legal-source chain inside the rail and C's exception grammar as the page lozenge + row elevation. Counterfactual and deletion test at frame 5r.
