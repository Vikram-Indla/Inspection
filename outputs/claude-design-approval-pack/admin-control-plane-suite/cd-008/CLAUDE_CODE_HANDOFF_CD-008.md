# CLAUDE_CODE_HANDOFF_CD-008.md
Controlled application order: semantic tokens → approved assets → shared components → route composition → localization → tests/evidence.
Design handoff only. implementation_authorized: false. A fresh read of apps/web/src/app/admin/packages/* is required before implementation to confirm fingerprint/usage/clone queries — flagged HANDOFF_BLOCKED throughout. The distinct-approver rule and published immutability are proven protected behavior: any diff touching them is out of scope. Codex must audit WIRING_MAP_CD-008.csv before any implementation authorization.
