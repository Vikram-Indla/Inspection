# IMPLEMENTATION_MANIFEST_CD-004_R2.yaml
task: CD-004
logical_screen: SCR-ADM-001
route: /admin
status: READY_FOR_MANDATORY_R2_REVIEW
implementation_authorized: false
gate_cycle_remaining: [independent_R2_review, sponsor_decision, independent_wiring_audit, explicit_authorization, clean_worktree_assignment]
selected_hypothesis: A_lifecycle_evidence_spine
signature_pattern: Configuration Evidence Spine (single; deletion-tested frame r2k)

file_changes:
  - path: apps/web/src/app/admin/page.tsx
    responsibility: control-plane home composition and per-source reads
    disposition: modify
    exact_change: >-
      Replace KPI wall + static live-database lozenge + two-link footer with:
      per-source result modelling for the existing six reads (verified-with-count /
      verified-zero / unavailable, per-source retry via route refresh or server action
      to be decided at wiring audit); evidence-spine table (semantics per
      ACCESSIBILITY_KEYBOARD_SPEC section 3); link-only family band (workflows, risk,
      gis, access — existing routes only); role-scope band from server-rendered roles;
      audit row. Remove SCR-ADM-* IDs from user copy.
    contract_ids: [SCR-ADM-001, MVP1-M09-001..030 gateway, RBAC-001..006, DSG-001]
    dependencies: [shell (FREEZE), astryx grammar (PRESERVE), i18n keys (LOCALIZATION_INVENTORY)]
    localization_keys: see LOCALIZATION_INVENTORY_CD-004_R2.csv (all admin.overview.r2.* keys)
    tests: >-
      per-source failure renders unavailable-not-zero; verified zero renders first-use
      empty; page renders with 1..6 sources failed; no health verdict string present;
      links resolve to existing routes; a11y semantics (h1, main, table caption, names)
    evidence: frames r2e-r2l + EVIDENCE_MANIFEST rows
    rollback: single-file revert of page.tsx (no schema/API change in this item)
    authorization_status: BLOCKED — sponsor approval + wiring audit + explicit authorization required
  - path: apps/web/src/lib/i18n.ts (generated key store)
    responsibility: add enumerated keys only
    disposition: modify
    exact_change: add keys listed in LOCALIZATION_INVENTORY with en/ar defaults; no other edits
    contract_ids: [DSG-SHELL-001]
    dependencies: []
    localization_keys: full inventory file
    tests: key presence + Arabic default rendering + directionality
    evidence: LOCALIZATION_INVENTORY_CD-004_R2.csv
    rollback: remove added keys
    authorization_status: BLOCKED
  - path: admin-family direct-route guard (location undecided — middleware vs layout guard vs policy)
    responsibility: unauthorized state enforcement (frame r2l-6)
    disposition: blocked
    exact_change: NONE — requires an authorized guard/policy decision; current middleware authenticates only and tests permit Inspector access
    contract_ids: [ERR-AUTH-001, RBAC-001..006, CD004-QG-04]
    dependencies: [governance decision]
    localization_keys: admin.overview.r2.unauthorized.*(inventory)
    tests: to be defined with the decision
    evidence: r2l-6 + truth memo
    rollback: n/a
    authorization_status: HANDOFF_BLOCKED
  - path: proposed provenance/draft-queue reads (DATA_TRUTH_LEDGER proposed rows)
    responsibility: richer per-family facts
    disposition: blocked
    exact_change: NONE in this CD — ledger specifies table/columns/filter/RLS/error contract if later authorized
    contract_ids: [CD004-QG-03]
    dependencies: [governance + implementation decision]
    localization_keys: none yet
    tests: n/a
    evidence: DATA_TRUTH_LEDGER proposed rows
    rollback: n/a
    authorization_status: HANDOFF_BLOCKED

forbidden_do_not_touch:
  - apps/web/src/components/Shell.tsx
  - apps/web/src/components/ShellClient.tsx
  - apps/web/src/lib/shell-navigation.ts
  - apps/web/src/components/NotificationBell.tsx
  - apps/web/src/app/tokens.css
  - design/astryx/d2/** (DESIGN_HISTORY_ONLY; never production CSS)
  - any new shell destination or placeholder route
  - any approve/publish/edit affordance on /admin
