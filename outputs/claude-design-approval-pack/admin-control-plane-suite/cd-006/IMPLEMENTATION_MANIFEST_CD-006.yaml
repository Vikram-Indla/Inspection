# IMPLEMENTATION_MANIFEST_CD-006.yaml
prompt_id: CD-006
screen_id: SCR-ADM-011
route: /admin/regulations (detail mode — in-page dossier panel, no new URL)
source_branch: unknown — no fresh repository read this session (frozen baseline used per protocol)
source_commit: unknown — same reason
dirty_worktree: unknown — same reason
design_frame_ids: [6e, 6f, 6g, 6h, 6i, 6j, 6t, 6s, 6o]
pngs: [CD-006_SCR-ADM-011_primary.png, CD-006_SCR-ADM-011_outlier.png]
status: READY_FOR_DESIGN_REVIEW
implementation_authorized: false

file_changes:
  - path: apps/web/src/app/admin/regulations/page.tsx
    before_responsibility: server data loading + flat list of regulations with raw nested clause/item arrays
    disposition: UPDATE
    after_responsibility: adds an in-page dossier panel (metadata header, clause navigator, persistent lifecycle rail with trace/compare/audit/publish-gate) opened from a selected regulation; preserves the existing list read
    design_node: frames 6e-6j, rail region
    code_target: new dossier region composed from the same nested read already performed by page.tsx (no new query — clause and mapped-item data is already fetched, per CD-005's frozen memo)
    protected_behavior: existing list read, existing RLS, existing audit triggers
    dependencies: [regulation_clauses read, inspection_items.clause_id read]
    rtl_theme_impact: full-document RTL dossier; dark/light equivalence per frames 6e-6h
    tests: dossier opens with correct clause set; trace renders unknown-not-zero on a failed mapped-item read; publish button disabled with accessible reason when any clause is unmapped
    rollback: single-file revert of page.tsx
  - path: apps/web/src/app/admin/regulations/Controls.tsx
    before_responsibility: create draft / add clause / direct publish controls beside the list
    disposition: UPDATE
    after_responsibility: publish control moves into the dossier's lifecycle rail, gated by a client/service validation check (unmapped-clause scan) in front of the existing publishRegulation action; add-clause control moves into the clause navigator
    design_node: 6e rail, publish button
    code_target: relocate existing exports; add a pre-publish validation function (pure, no new schema)
    protected_behavior: existing createDraftRegulation, addClause, publishRegulation server actions unchanged in signature
    dependencies: [actions.ts]
    rtl_theme_impact: gate copy localized per LOCALIZATION_INVENTORY
    tests: publish blocked when >=1 clause has zero mapped items; publish allowed and locks the record when all clauses are mapped
    rollback: revert Controls.tsx; server actions untouched
  - path: apps/web/src/app/admin/regulations/actions.ts
    before_responsibility: createDraftRegulation, addClause, publishRegulation (direct draft->published update)
    disposition: PRESERVE
    after_responsibility: unchanged — no maker-checker, no version-chain, no dependency-validation RPC added; those remain HANDOFF_BLOCKED
    design_node: n/a
    code_target: n/a
    protected_behavior: exact current signatures and RLS behavior
    dependencies: []
    rtl_theme_impact: none
    tests: existing tests must still pass unmodified
    rollback: n/a — no change proposed
  - path: apps/web/src/lib/i18n.ts
    before_responsibility: Arabic lookup mechanism over ui_strings
    disposition: PRESERVE
    after_responsibility: unchanged — new copy is added as ui_strings rows (data), not a code change; i18n.ts is a lookup layer, never a generated key store
    design_node: LOCALIZATION_INVENTORY_CD-006
    code_target: n/a (data change only, HANDOFF_BLOCKED pending governance)
    protected_behavior: existing lookup mechanism
    dependencies: [ui_strings rows]
    rtl_theme_impact: n/a
    tests: n/a
    rollback: n/a

forbidden_do_not_touch:
  - apps/web/src/components/Shell.tsx / ShellClient.tsx / shell-navigation.ts / astryx.css global rules
  - apps/web/src/app/tokens.css
  - inventing a new route for regulation detail
  - inventing maker-checker, version-chain, or dependency-validation schema/RPC
