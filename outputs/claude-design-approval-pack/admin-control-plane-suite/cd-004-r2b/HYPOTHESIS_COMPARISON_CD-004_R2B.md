# HYPOTHESIS_COMPARISON_CD-004_R2B.md
Equal-fidelity rebuild per review: same role (compliance_admin), same dataset, same hard case (Enforcement Library read unavailable), Arabic-first, same shell, 1440 decision zone. Frames r2a/r2b/r2c.

A — Lifecycle evidence spine (SELECTED): per-family rows binding object -> per-source read state -> schema-proven lifecycle -> provenance -> one door.
B — Exception-first governance ledger: exceptions lead; verified families compress to a summary line.
C — Control-plane matrix: families x facts grid.

| Criterion | A | B | C |
|---|---|---|---|
| Decision time (30-second question) | one scan per family | fastest on bad days only | cell-by-cell reading |
| Assumption visibility | per-row proven lifecycle | hidden behind "all verified" | per-cell but normalized |
| Irreversible-error risk | none acts; module ownership clearest | none acts | none acts |
| Arabic scan quality | natural RTL row grammar | good | needs smallest type at 1024 |
| Role clarity | scope band integral | bolted on | bolted on |
| Accessibility | one semantic table | two layouts (empty vs exceptions) | widest table, hardest reflow |
| Implementation risk | maps 1:1 to six per-source reads | conditional layout logic | most cells to state-manage |
| Signature cost | one pattern | one pattern + empty-state variant | one pattern, heaviest |

Decision: A retained. B's exception emphasis survives as the page lozenge + elevated failed row; C's column discipline survives as the row anatomy. Evidence-based; no scores.
