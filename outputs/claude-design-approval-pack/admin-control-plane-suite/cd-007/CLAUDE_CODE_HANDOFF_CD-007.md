# CLAUDE_CODE_HANDOFF_CD-007.md
Controlled application order: semantic tokens → approved assets → shared components → route composition → localization → tests/evidence.
Design handoff only. implementation_authorized: false. Before any implementation, a fresh repository read of apps/web/src/app/admin/items/{page.tsx,Controls.tsx,actions.ts} is required to confirm the item schema — this design proceeded without one and flags every schema-dependent element as HANDOFF_BLOCKED. Codex must audit WIRING_MAP_CD-007.csv before any implementation authorization.
