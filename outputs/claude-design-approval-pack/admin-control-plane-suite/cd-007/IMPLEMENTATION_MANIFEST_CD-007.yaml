# IMPLEMENTATION_MANIFEST_CD-007.yaml
prompt_id: CD-007
screen_id: SCR-ADM-020
route: /admin/items
source_branch: unknown — no fresh repository read this session
source_commit: unknown — same reason
dirty_worktree: unknown — same reason
design_frame_ids: [7e, 7f, 7g, 7h, 7i, 7j, 7t, 7s, 7o]
pngs: [CD-007_SCR-ADM-020_primary.png, CD-007_SCR-ADM-020_outlier.png]
status: READY_FOR_DESIGN_REVIEW
implementation_authorized: false

file_changes:
  - path: apps/web/src/app/admin/items/page.tsx
    before_responsibility: unknown — not inspected this session
    disposition: UPDATE (pending schema confirmation)
    after_responsibility: composes the semantic catalogue table + attached runtime preview strip; exact query shape depends on confirming item schema at wiring audit
    design_node: frames 7e-7j
    code_target: HANDOFF_BLOCKED — exact columns not confirmed this session
    protected_behavior: existing item reads, RLS, audit triggers (assumed present, not verified)
    dependencies: [inspection_items.clause_id (proven cross-reference from CD-005/006)]
    rtl_theme_impact: full dossier RTL/dark/light per 7e-7h
    tests: HANDOFF_BLOCKED pending schema confirmation
    rollback: n/a until schema confirmed
  - path: apps/web/src/app/admin/items/Controls.tsx
    before_responsibility: unknown — not inspected this session
    disposition: UPDATE (pending confirmation)
    after_responsibility: duplicate-resolution and conditional-rule controls surfaced from the catalogue row
    design_node: 7e/7g "Resolve duplicate code" button
    code_target: HANDOFF_BLOCKED
    protected_behavior: unknown
    dependencies: []
    rtl_theme_impact: localized per LOCALIZATION_INVENTORY_CD-007
    tests: HANDOFF_BLOCKED
    rollback: n/a
  - path: apps/web/src/app/admin/items/actions.ts
    before_responsibility: unknown — not inspected this session
    disposition: PRESERVE (default until confirmed otherwise)
    after_responsibility: unchanged unless confirmed schema requires new fields
    design_node: n/a
    code_target: HANDOFF_BLOCKED
    protected_behavior: assume-preserve until inspected
    dependencies: []
    rtl_theme_impact: none
    tests: n/a
    rollback: n/a

forbidden_do_not_touch:
  - frozen shell files (Shell.tsx / ShellClient.tsx / shell-navigation.ts / tokens.css)
  - inventing exact item schema columns not confirmed at wiring audit
  - inventing violation-code mapping for the preview strip's "may create a violation note" note
