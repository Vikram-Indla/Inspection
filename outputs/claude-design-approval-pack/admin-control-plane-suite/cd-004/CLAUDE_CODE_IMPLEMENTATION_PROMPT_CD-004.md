# CLAUDE_CODE_IMPLEMENTATION_PROMPT_CD-004.md
> **NOT EXECUTABLE.**
> Blocked until, in order: (1) mandatory independent Codex review, (2) consolidated P0/P1 correction, (3) R2, (4) sponsor approval, (5) independent wiring audit, (6) separate explicit implementation authorization + a clean dedicated worktree/branch. If ANY is missing, STOP.

## Identity
Implement only the sponsor-approved CD-004 / SCR-ADM-001 /admin control-plane home (Chapter 1 of TASK-DESIGN-ADMIN-SUITE-001).

## Read first
outputs/claude-design-approval-pack/admin-control-plane-suite/cd-004/: manifest (confirm implementation_authorized flipped true), approved design file, component map, wiring map, state matrix, acceptance checklist, truth memo.

## Scope after authorization
- Rework apps/web/src/app/admin/page.tsx (minimum candidate path) into the governance evidence spine: per-family rows (object · per-source verification with real count only on verified reads · schema-proven lifecycle wording · provenance · one real route link), role-scope band, audit link band.
- Replace Promise.all fate-sharing with per-source outcomes; a failed read renders "unavailable — count unknown, not zero" with per-source retry; verified zero renders as first-use empty. Never a platform/health verdict; the read timestamp is a fact.
- Remove SCR-ADM-* IDs and the static "live database" lozenge from user copy.
- Resolve at wiring audit: admin-family route guard for the unauthorized state; exact i18n keys (Arabic-first strings); tests for every wiring row and states 1–10.

## Hard prohibitions
- No edit to FREEZE files (Shell, ShellClient, shell-navigation, NotificationBell, tokens.css); no new destination or placeholder route; no approve/publish/edit affordance on the home; no invented age/SLA/threshold/provider/consumption/ownership data; no d2/admin.css in production; no commit/push/merge/deploy or main modification unless separately authorized; preserve dirty worktree.
- STOP rather than invent any unresolved policy, guard, or data source.

## Evidence before claiming done
Per-source failure/negative tests; verified-zero vs unavailable distinction; route-guard behavior; Arabic RTL + dark/light + constrained-width screenshots of the same state; keyboard/focus/announcement checks per the 4o spec. Report gaps as gaps.
