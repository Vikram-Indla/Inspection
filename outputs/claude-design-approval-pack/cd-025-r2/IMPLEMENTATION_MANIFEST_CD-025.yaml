# IMPLEMENTATION_MANIFEST_CD-025.yaml
# CD-025 / SCR-WEB-150 / P03 — Plan Review & Publish
# Imported from Claude Design project 20cb0dce-94f1-4423-b923-00d6fd0d2c24
# (file "CD-025 Plan Review and Publish.dc.html", outputs/cd-025-r2/) on 2026-07-14.
# R2 design handoff, reconciled against live repo truth on import (see baseline_observed).

meta:
  design_id: CD-025
  logical_screen: SCR-WEB-150
  process: P03
  name: Plan Review & Publish
  persona: Planner
  acceptance: [DSG-020, DSG-A11Y-001, DSG-CODE-001]
  design_file: "CD-025 Plan Review and Publish.dc.html"
  implementation_authorized: true       # sponsor (Vikram Indla) directed implementation on branch, 2026-07-14
  status: IMPLEMENTED_ON_BRANCH         # feat/cd-025-plan-review-publish; not merged to main

baseline_observed:
  # Reconciled at import. Current repo is AHEAD of the R2 design baseline; current
  # evidence wins. The design's largest HANDOFF_BLOCKED legs are RESOLVED in code.
  ref_at_import: "main @ 9360fc9"
  resolved_since_r2:
    - "Governed route LIVE: /planning/bulk/review (gate CD-021-P02-relocation-approval)."
    - "plan_validated_state migration LIVE (20260714091726) — Draft→Validated→Published real."
    - "Atomic guarded publisher LIVE: publish_bulk_plan (20260714091727) returns plan_id."
    - "Automatic assignment is FIRST-AVAILABLE-IN-WINDOW server-side (no longer round-robin)."
  still_handoff_blocked:
    - "planning-specific approver / maker-checker configuration"
    - "notification provider delivery / receipt / acceptance"
    - "durable success receipt / download"
    - "policy-based freshness duration / stale threshold"
    - "lost/expired staged-review recovery + guaranteed return-to-edit context restore"

implemented:
  route: "/planning/bulk/review"
  server_page: "apps/web/src/app/planning/bulk/review/page.tsx"    # RBAC-007 Planner gate + resolved strings
  client: "apps/web/src/app/planning/bulk/review/ReviewClient.tsx" # workspace body
  styles: "apps/web/src/app/planning/bulk/review/review.css"       # scoped cd-* classes, tokens only
  actions: "apps/web/src/app/planning/bulk/actions.ts"             # validateBulkPlan (preview) + publishBulkPlan (authoritative)
  components:
    - PlanReviewWorkspace   # blocker-first body: context→readiness→targets→evidence→ledger→corrections→action
    - ReadinessRail         # role=alert error-summary; each blocker links a Fix to its object/row
    - PublishConsequenceLedger  # signature pattern: 4 groups bound to retained scope, live recalc
    - ScopeReductionControl # "Remove excluded factories" — explicit named duplicate removal
    - PublishActionBar      # single native publish; disabled reason via aria-describedby
  shell: "apps/web/src/components/Shell.tsx (PROTECTED — used as-is; provides responsive drawer + skip link)"

truthful_copy_fixes:
  - { from: "Auto (round-robin)", to: "Automatic — chosen at publish (first eligible available)" }
  - { from: "contact support (invented destination)", to: "neutral retry; review flagged items" }
  - { publisher: "returned plan_id captured → optional read-only /planning/plans/:id link on success (S26)" }

evidence:
  spec: "apps/web/e2e/cd-025-plan-review-publish.spec.ts (11 read-only tests, PASS)"
  regression: "apps/web/e2e/cd-021-bulk-targeting.spec.ts (updated for CD-025 review workspace, PASS)"
  screens: "product-contract/evidence/screens/cd-025-plan-review-v1/ (review-primary, review-ar-rtl, review-narrow-412)"
  wiring_audit: "outputs/claude-design-approval-pack/cd-025-r2/CD-025_WIRING_AUDIT.md (DEC-012, 14 legs)"
  build: "next build PASS; tsc --noEmit PASS"

not_invented:
  - "No maker-checker/approver policy, validation-version token, freshness threshold, bulk-size policy."
  - "No skills/cert/territory/capacity eligibility (shown as 'Not evaluated')."
  - "No provider delivery/acceptance, durable receipt, typed-confirmation phrase, or support destination."
  - "publish_bulk_plan remains the authority; validateBulkPlan is a preview only."
