# CD-012–019 R2 Review and Required R3 Correction

## R2 verdict

**BLOCKED.** R2 materially improves the truth model, especially CD-014, CD-016, CD-017, and CD-019. It is not acceptably complete because it again declares readiness while three mandatory quality gates remain partial.

## Keep from R2

- CD-014 now reflects direct `engine_settings` configuration rather than an invented approval workflow.
- CD-016 is a non-actionable route/contract boundary rather than a fake operating rule studio.
- CD-017 is a read-only holdings view and makes the effective-access explanation non-conclusive.
- CD-018 acknowledges its real action set.
- CD-019 removes fake event/correlation totals, export, reveal, and tamper claims.
- Plain-language unavailable boundaries are materially better than raw seam IDs across every primary table.

## R3 blocking corrections

1. **No partial PASS.** `PACKAGE_PREFLIGHT.md` must be FAIL while any native export, source receipt, or equal-fidelity candidate requirement is incomplete. “Ready for design review” cannot coexist with those gaps.

2. **Actual raster dimensions.** All R2 PNGs are 924×540. Files labelled 1440/1024/412 are not native exports. The 412 localization frame is still an unusable tiny layout in large blank canvas. Render and deliver exact-size lossless images—or submit a truthful FAIL package without claiming completion.

3. **Capture the selected state visibly.** Every export must show the relevant state’s status, action availability, recovery, and consequence in the viewport. Fit-to-preview captures do not qualify.

4. **Source discovery is mandatory.** R2 still says no repository source was opened. Open the exact code/contract files, record branch/commit/worktree, and cite paths/symbols. No screen may rely on `STATED_BY_CORRECTION` for an implementation hand-off.

5. **Candidates must be rendered.** R2 documents alternates but does not render three equal-fidelity candidates per screen. Supply them at native 1440 with the same scope/state fidelity as the selected candidate.

6. **Clean final archive.** The submitted ZIP contains master/R1/R2/historical folders and unrelated files. The R3 ZIP itself must have only `outputs/cd-012-019-r3/` at root.

7. **R2 visual refinements.** Keep the CD-016/017 boundaries but reduce technical microcopy in the main surface. Put full prerequisite lists in a detail/evidence panel. Strengthen dark-theme hierarchy by increasing distinction between page background, cards, table headers, muted labels, and disabled states. In light theme verify border and muted-text contrast independently. Rebuild each 412 view as a true narrow composition, not a compressed desktop arrangement.

8. **No unproven value semantics.** A `COMPUTED_FROM_*` label is allowed only when the exact opened source inputs and arithmetic are named. If a missing risk input is not proven to score zero, do not show it scoring zero. Correct CD-014’s behavior accordingly.

## Paste into Claude Design as one correction prompt

Correct the consolidated **CD-012 through CD-019 Admin Control Plane** package to R3. R2 is blocked. Do not make a cosmetic revision and do not return “PASS with partials.”

### Hard release gate

The final package is PASS only if every requirement below is met. If any item cannot be met in the current environment, set `PACKAGE_PREFLIGHT.md` to **FAIL**, explain the precise blocker, and do not describe the package as ready for design review.

### A. Source truth before pixels

Open the repository authority and all screen-specific code before designing. Record branch, commit, dirty working tree, exact source paths, exported symbols, data reads/writes, server actions, validation guards, RLS/role boundaries, audit behavior, state transitions, error behavior, and test seams.

Required route sources include:

- `apps/web/src/app/admin/workflows/{page,actions,Controls}.tsx`
- `apps/web/src/app/admin/risk/{page,actions}.ts(x)`
- `apps/web/src/app/admin/gis/{page,GisStudio,actions}.ts(x)`
- `apps/web/src/app/admin/access/page.tsx`
- `apps/web/src/app/admin/localization/{page,Manager,actions}.ts(x)`
- `apps/web/src/app/admin/audit/page.tsx`
- the relevant product contract, decisions, migrations/RLS, acceptance/state matrices, and shared shell sources.

`source-receipt.md` must cite what was actually opened. Use only three evidence classes:

- `PROVEN_SOURCE` — exact path and symbol opened;
- `COMPUTED_FROM_<SOURCE>` — exact opened inputs and formula/derivation;
- `HANDOFF_BLOCKED_<SEAM>` — unavailable or unresolved.

Do not use `STATED_BY_CORRECTION` as a build hand-off class. Do not mark fixture data as proven. Do not invent a fallback behavior such as “missing risk input scores zero” unless source proves it.

### B. Preserve R2’s corrected model

- CD-012: real config-version library, draft/propose/save/publish, maker-checker and immutability; no graph/test/case/stale/notification metric without source.
- CD-013: current JSON payload editor is the delivered model. Canvas, graph analysis, replay, test store, calendar and notifications stay non-executable contract proposals until approved.
- CD-014: direct risk settings read/save with real weight validation; no invented approval lifecycle or source-freshness/backtest promise.
- CD-015: official coordinates and real geofence-radius edit; no implied map/geocoder/boundary/evidence-sync provider.
- CD-016: no route, therefore no active notification/SLA controls. Retain a concise unavailable boundary only.
- CD-017: read-only holdings/roles; no change, submit, publish, or conclusive policy simulator without approved source contracts.
- CD-018: use the real localization action set and its true validation/history/restore semantics.
- CD-019: use the real RLS-scoped audit reader, filters, pagination, and before/after detail; no totals, reveal, export, correlation, retention, or hash-chain claim without source.

### C. Native visual evidence—no substitute

Export true native, lossless PNGs at exactly these pixel dimensions:

- 1440 px desktop dark EN/LTR;
- 1440 px desktop light EN/LTR;
- 1440 px desktop Arabic/RTL;
- 1024 px tablet;
- 412 px narrow;
- every screen’s selected primary and critical outlier;
- every required loading, empty, validation, unauthorised, read-only, stale/degraded, offline/recovery, and screen-specific state.

Do not deliver 924×540 preview captures with a design-width label. Do not scale a desktop into empty canvas for 412. Every PNG must be readable at its native size and visibly show the selected state, status, active/disabled control, recovery, audit/immutability consequence, and next action.

Include `CAPTURE_MANIFEST_CD-012-019_R3.csv` with exact actual raster dimensions, SHA-256, capture command or deterministic selector, viewport, state, theme, language, timestamp, and a file existence check. The preflight must verify the image dimensions from the final files rather than trusting filenames.

### D. Real exploration and visual quality

For each of the eight screens, render three complete equal-fidelity 1440 candidates—not descriptions—and select one against operational comprehension, runtime truth, accessibility, dark/light contrast, Arabic/RTL, density, and responsive integrity. Keep R2’s plain-language unavailable boundary, but move technical dependency detail to an evidence/inspect panel. Do not let developer labels, raw seam IDs, or fixture chips become the primary visual language.

Rework theme treatment independently:

- Dark: distinguish canvas, cards, tables, disabled state, muted metadata, and focus without relying on faint outlines or tiny mono text.
- Light: verify text, borders, status fills, empty state, table headers, and disabled control contrast at AA.
- Narrow: create a true task-first 412 information hierarchy; no desktop shell scaled down, no blank canvas, no unreadable table.
- RTL: use genuine Arabic, correct logical mirroring, bidi isolation for IDs/dates, full keyboard/focus order, and visible validation/blocked states.

Every visual carries `DESIGN FIXTURE — NOT LIVE DATA` without obscuring content. Fixture counts or example records must never be styled as live telemetry.

### E. Final package contract

Submit one ZIP with only `outputs/cd-012-019-r3/` at the archive root. Include source receipt, runtime truth ledger, per-screen candidate decision matrices, final native frames, all-state matrix, accessibility/RTL and dark/light QA, exact wiring map, backend readiness matrix, capture manifest, package inventory, and final preflight.

The preflight is PASS only if the final ZIP itself proves: one root; no contamination; repository sources opened and cited; three full candidates per screen; exact native dimensions; readable state evidence; correct runtime action mappings; and no unsupported provider/policy/metric/action rendered as live. Otherwise return FAIL. Do not begin frontend or backend implementation.
