# Claude Design Progressive Correction Prompt — CD-025 R3 → R4

## Where this prompt goes

Paste this entire document into **Claude Design**, in the project containing the submitted CD-025 R3 design.

This is not a Claude Code prompt.

## Task identity and boundary

Revise the package as **CD-025 / SCR-WEB-150 / P03 Plan Review & Publish R4**.

This is a package-synchronization and evidence correction. It is not application implementation and not another visual exploration.

Do not edit application code, migrations, database data, tests, product-contract files or Git history. Do not implement, commit, push, merge, deploy, reset, clean, stash, discard or modify `main`.

Keep:

`implementation_authorized: false`

Every Claude Code-facing file must begin with:

`DO NOT EXECUTE UNTIL SPONSOR DESIGN APPROVAL AND INDEPENDENT CODEX WIRING AUDIT`

## Preserve the successful R3 design

Do not redesign or weaken:

- the root R3 `.dc.html` composition;
- the blocker-first review workspace;
- Publish Consequence Ledger as the single page-specific signature pattern;
- the 12 selected → 2 named duplicate exclusions → 10 retained count model;
- 10 visits, 10 assignments and 10 queued notification rows;
- 3 manual plus 7 automatic assignments;
- first eligible available Inspector chosen server-side at publish;
- guarded atomic bulk and single publishers;
- full rollback and no partial/optimistic success;
- neutral failures;
- queued never meaning delivered, received, opened or accepted;
- immediate 10 published, 0 draft and 0 completed continuity;
- all 28 states;
- read-only plan detail;
- unresolved route/lifecycle/CD-024→CD-025 ownership as `HANDOFF_BLOCKED`;
- the grouped role-scoped Planner shell now present in the R3 root design;
- the focusable-element drawer model now present in the R3 root design;
- Arabic RTL, dark/light and desktop/1024/412 content treatment.

Do not introduce another signature pattern. Do not reopen page composition for P2 preferences.

## Why R4 is mandatory

The submitted archive is a hybrid:

- the root `.dc.html`, `cd25-stage.js`, `cd25-annot.js` and three mapping files contain R3 corrections;
- the standalone, PNG evidence, state matrix, acceptance checklist, provenance and both Claude Code-facing files remain R2;
- the R3 manifest points to a standalone file that does not exist;
- stale R2 files still claim `setup/Inspection`, absent atomic publishers, round-robin assignment, absent Validated persistence, a flat shell and a defective `NarrowAppBar`;
- the loose drawer screenshots still show the obsolete flat shell;
- the R3 provenance says the requested commit was not resolvable even though the governing correction required an exact baseline or a stop.

Do not solve this by relabelling files. Regenerate and synchronize the complete package.

## Baseline rule

Inspect the supplied repository at exact commit `9360fc9` and record the actual branch/ref and commit.

Read at minimum:

1. `product-contract/00_START_HERE.md`
2. `product-contract/CURRENT_STATE.md`
3. `product-contract/GATE_STATUS.md`
4. `product-contract/execution/CURRENT_SLICE.yaml`
5. `product-contract/execution/TASK_ROUTER.yaml`
6. `product-contract/governance/OPEN_DECISIONS.yaml`
7. `design/claude-design-mvp1/00_START_HERE.md`
8. `design/claude-design-mvp1/MANIFEST.yaml`
9. `apps/web/src/components/Shell.tsx`
10. `apps/web/src/components/ShellClient.tsx`
11. `apps/web/src/lib/shell-navigation.ts`
12. `apps/web/src/app/astryx.css`
13. `apps/web/src/app/planning/bulk/review/page.tsx`
14. `apps/web/src/app/planning/bulk/review/ReviewClient.tsx`
15. `apps/web/src/app/planning/bulk/actions.ts`
16. `apps/web/src/app/planning/single/actions.ts`
17. `apps/web/src/app/planning/plans/[id]/page.tsx`
18. `supabase/migrations/0031_cd023_assignment_overlap_guard.sql`
19. `supabase/migrations/20260714091726_plan_validated_state.sql`
20. `supabase/migrations/20260714091727_planning_publish_guards.sql`
21. `outputs/claude-design-approval-pack/CD-025_DESIGN_REVIEW_R3.md`

If exact commit inspection is unavailable, do not claim that another branch is identical. Return:

`BASELINE_UNAVAILABLE — CD-025 R4 NOT GENERATED`

## Binding runtime truth

Every R4 artifact must agree that:

- `publish_bulk_plan` and `publish_single_visit` are existing guarded atomic publishers;
- caller RLS is preserved and authoritative mutable conditions are rechecked in the transaction;
- factory advisory locks and the canonical assignment-overlap guard protect concurrency races;
- automatic assignment ignores the caller pool and selects the first eligible Inspector available in the window;
- assignment is not round-robin, AI, optimization, capacity scoring, suitability ranking, territory scoring or risk scoring;
- both publishers persist `Draft → Validated → Published`;
- notification rows are queued in the transaction;
- successful mutations receive append-only audit records;
- publisher failure rolls back the complete write set;
- bulk success continues to `/visits`;
- single success continues to `/visits/:visitId`;
- `/planning/plans/:id` is read-only;
- direct Planner publication is supported;
- final CD-025 route, persisted pre-publication review ownership and CD-024→CD-025 handoff remain unresolved.

## Mandatory synchronization correction

Create one clean `outputs/cd-025-r4/` package. It must contain:

1. `CD-025 Plan Review and Publish.dc.html`
2. `CD-025 Plan Review and Publish.standalone.html`
3. `IMPLEMENTATION_MANIFEST_CD-025.yaml`
4. `COMPONENT_MAP_CD-025.csv`
5. `WIRING_MAP_CD-025.csv`
6. `STATE_MATRIX_CD-025.csv`
7. `ACCEPTANCE_CHECKLIST_CD-025.md`
8. `RESEARCH_PROVENANCE_CD-025.md`
9. `CLAUDE_CODE_HANDOFF_CD-025.md`
10. `CLAUDE_CODE_IMPLEMENTATION_PROMPT_CD-025.md`
11. `PACKAGE_INVENTORY_CD-025.csv`
12. every required PNG evidence export.

The final submitted archive must contain the R4 package only for governed CD-025 deliverables. Do not include stale R2/R3 handoff files, stale screenshots or loose evidence folders that contradict R4.

`PACKAGE_INVENTORY_CD-025.csv` must list for every deliverable:

- relative path;
- revision (`R4`);
- baseline commit;
- design ID;
- screen ID;
- status;
- whether implementation-facing;
- synchronization check result.

No R4 manifest may reference a missing path.

## Exact shared-shell evidence

Use the corrected R3 root design and prove the current Planner shell:

- Command group containing Factory 360;
- Inspection group containing Planning and Visit Management;
- Planning active;
- correct icons and group disclosures;
- navigation search;
- theme control;
- notification control;
- own-account trigger with identity and role;
- language and sign-out inside the account menu;
- desktop collapse control and collapsed state;
- skip-to-content;
- real narrow drawer behavior.

Do not restore the flat shell, sidebar-footer language/sign-out or `NarrowAppBar`.

## Keyboard and drawer evidence

In both English LTR and Arabic RTL at 412px, prove:

1. drawer closed;
2. drawer open;
3. focus moves to the first actual enabled focusable control;
4. every visible enabled drawer control is reachable in DOM order;
5. reverse Tab wraps from the first to the last;
6. forward Tab wraps from the last to the first;
7. Escape closes the drawer;
8. focus returns to the menu trigger.

Document this in the acceptance checklist and state matrix. Do not use `cd-drawerfirst` or `cd-drawerlast` sentinels.

## Required visual evidence

Export at least:

- ready — dark English desktop;
- blocked-12 — dark English desktop;
- blocked-12 — light English desktop;
- blocked-12 — dark Arabic RTL desktop;
- blocked-12 — light Arabic RTL desktop;
- scope reduction — dark English desktop;
- concurrent rollback — dark English desktop;
- bulk failure — dark English desktop;
- single failure — dark English desktop;
- bulk success — dark English desktop;
- read-only plan detail — dark English desktop;
- 1024px shell/content state;
- 412px English drawer closed;
- 412px English drawer open;
- 412px Arabic RTL drawer closed;
- 412px Arabic RTL drawer open;
- desktop collapsed navigation;
- account menu showing language and sign-out ownership;
- route/lifecycle annotation.

Every filename must include `CD-025`, `SCR-WEB-150`, state, theme, language, width and `R4` where relevant.

## Claude Code-facing files

Regenerate both files as R4. They must:

- be based on the exact verified baseline;
- preserve the existing guarded publishers and shared shell;
- never name `NarrowAppBar`;
- never describe current publication as sequential;
- never describe current assignment as round-robin;
- never claim Validated persistence is absent;
- never ask Claude Code to recreate or conditionally replace protected behavior;
- keep route/lifecycle/CD-024→CD-025 ownership `HANDOFF_BLOCKED`;
- require sponsor approval and an independent Codex wiring audit before execution;
- keep `implementation_authorized: false`.

## R4 acceptance test

Do not return R4 until all are true:

- every governed file is present under `outputs/cd-025-r4/`;
- all files identify R4, CD-025 and SCR-WEB-150;
- all files cite the same exact baseline commit;
- no governed R4 file cites `setup/Inspection` as current truth;
- no governed R4 file says atomic publishers or Validated persistence are absent;
- no governed R4 file presents current automatic assignment as round-robin;
- no manifest path is missing;
- standalone and `.dc.html` show the same grouped shell and page state;
- PNGs are rendered from R4 rather than copied from R2;
- all 28 states remain individually selectable;
- the 12→10→10 count model is consistent;
- Arabic RTL, dark/light, desktop, 1024 and 412 are evidenced;
- drawer keyboard evidence covers first focus, both wraps, Escape and focus return;
- failures remain neutral and atomic;
- notification rows remain queued-not-delivered;
- unresolved ownership legs remain `HANDOFF_BLOCKED`;
- both Claude Code-facing files carry the execution prohibition;
- `implementation_authorized: false` is explicit;
- final status is `READY_FOR_DESIGN_REVIEW_R4`.

Return the complete clean R4 archive for one final sponsor/Codex review. Do not implement.
