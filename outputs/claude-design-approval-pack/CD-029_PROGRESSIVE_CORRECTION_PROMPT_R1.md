# Claude Design Correction Prompt — CD-029 R1 → R2

Paste into the existing **CD-029 Level 2 Review Workspace** project. This is package synchronization only; do not implement or redesign.

Keep `implementation_authorized: false` and the required Claude Code first line:

`DO NOT EXECUTE UNTIL SPONSOR DESIGN APPROVAL AND INDEPENDENT CODEX WIRING AUDIT`

Preserve the Finding Trace Chain, immutable/audit truth, page-load mutation and non-atomic decision `HANDOFF_BLOCKED` labels, exact return validation, media/claim/reassign/error-map blocks, hypothesis frames, Arabic/theme/responsive evidence, and frozen shared shell.

## Correct exactly these defects

1. Create `outputs/cd-029-r2/` as a self-contained package. Include every required asset used by the `.dc.html` and standalone, especially `support.js` if referenced. Add every included path to `PACKAGE_INVENTORY_CD-029.csv`; verify every manifest/inventory reference resolves inside R2.
2. Update all manifest, handoff and implementation-facing paths/revision labels from R1 to R2.
3. Submit a ZIP whose root contains **only** `outputs/cd-029-r2/`. Exclude CD-025, CD-026, CD-027, CD-029 R1, root duplicate files, uploads and historical prompts.

Return `READY_FOR_DESIGN_REVIEW_R2`. Do not sponsor-approve, implement, commit, push or alter application code.
