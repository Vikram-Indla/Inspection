# Claude Design Web-First Master Prompt V4 — Quality Ratchet

TASK-WEB-SHELL-001 is sponsor-accepted and frozen. Reuse its role-scoped grouped sidebar, sticky topbar, navigation search, theme, notifications, language, account/sign-out, desktop collapse and accessible mobile drawer exactly. Do not redesign global chrome or alter the governed business-tab dispositions.

Before composing, read `DESIGN_QUALITY_RATCHET_V4.md`. Start with repository/runtime discovery, a route-and-ownership memo and a critique of the current screen. If the proposed screen writes through a read-only route, depends on unverified data/provider behavior, or collides with a governed screen identity, stop the implementation handoff and mark the exact leg `HANDOFF_BLOCKED`.

Design the page's inspection decision, not a generic dashboard. Show object continuity, provenance, version, lifecycle state, scope, freshness, consequence, hard cases and recovery. Produce three equal-fidelity decision-zone hypotheses. Compare them against decision time, error prevention, evidence visibility, Arabic/RTL, keyboard burden and runtime truth. Select one and prove what is lost when its single signature interaction is removed. Cosmetic variants do not qualify.

Inherit the accepted Saqeel family grammar and introduce at most one new signature pattern. Use realistic inspection data. Arabic is the fresh-session default with full-document RTL; dark/light, desktop/narrow, mixed-direction content, keyboard/focus, screen-reader status and reduced motion are mandatory. A map, timeline or visual board must have an equivalent semantic list/table path.

Every action must map UI → server action/route → validation/guard → canonical transition → table/RPC/storage/provider → RLS/role → audit → side effect → tests/evidence. Never imply recommendation quality, eligibility, travel time, delivery, atomicity or audit without runtime proof. Missing legs remain `HANDOFF_BLOCKED` through sponsor review.

Return research adoption/rejection, route truth, current-screen critique, hypothesis comparison, selected high-fidelity states, decision-zone close-up, inheritance ledger, accessibility/RTL/responsive behavior, exact file map, wiring map, acceptance evidence and `READY_FOR_DESIGN_REVIEW`. Never self-score or self-approve.

