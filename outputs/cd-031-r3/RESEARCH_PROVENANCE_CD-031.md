# RESEARCH_PROVENANCE_CD-031.md — R3
DO NOT EXECUTE UNTIL SPONSOR DESIGN APPROVAL AND INDEPENDENT CODEX WIRING AUDIT

Non-product. Three primary sources; observed / adopted / rejected. Informs information architecture and provenance treatments only — no runtime behaviour inferred.

## 1. Enterprise asset / facility 360 dossier pattern
- Observed: a trustworthy 360 keeps identity provenance persistent and separates registry-owned facts from operational history and derived scores.
- Adopted: persistent provenance-led aside; source-labelled Spatial Case Timeline; derived risk labelled with version; per-section failure isolation.
- Rejected: a CRM 360 card wall, a decorative risk gauge and a live-map hero the route cannot back.

## 2. Saudi public-service bilingual registry pattern
- Observed: full-document RTL, mixed-direction identifiers/dates via bidi isolation, read-only source-owned identity with sync provenance.
- Adopted: bdi isolation for factory/CR/licence codes, coordinates and dates; read-only identity from source with an explicit sync timestamp.
- Rejected: colour-only status; editable source-owned identity fields.

## 3. Accessibility / RTL authoring guidance (list-equivalent timeline + non-color status)
- Observed: ordered-list timelines with non-color markers, per-region landmarks, isolated section failures, reduced-motion equivalents.
- Adopted: the Spatial Case Timeline as an ordered list with glyph markers; reduced-motion static equivalent; per-section failure banners; skip link + landmarks.
- Rejected: an animation-only or map-only timeline with no list fallback.

## Baseline
BASELINE_REVERIFY_REQUIRED — the /factories/:id sources (page.tsx, Controls.tsx, actions.ts) were read at main this session. No exact-baseline equivalence is claimed; exact code/wiring verification is deferred to independent Codex audit.
