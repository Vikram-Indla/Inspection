# MIM Inspection Platform — Build Plan (real delivery)

Authorized 2026-07-11 (HUMAN_APPROVALS.yaml: G5 PASS · G6 accepted · build authorized).
Stack per DEC-010: Next.js/React/TS (`apps/web`) · offline-first PWA for iPad · Supabase (Postgres/Auth/Storage/Realtime, RLS = RBAC matrix). Design authority: `design/retired-predecessor/*` — every screen implements its frame 1:1. Acceptance contract non-negotiable: each slice certifies against its AC rows + EV proofs before "done".

## Phases to completion (9 build slices after today's B1 start)

| Slice | Scope | Certifies |
|---|---|---|
| **B1 ✓ COMPLETE (2026-07-11)** | Migrations 0001–0004 LIVE (30 tables, 54 policies, 44 triggers incl. immutability guards + append-only audit), contract seed (SBC-801, 5 items, published PKG-FS, violation/penalty mappings, 4 factories), 5 role-scoped users, auth (middleware+login), live screens: /admin, /admin/regulations, /planning, /visits. Smoke-tested: middleware redirect ✓ login ✓ RLS (planner sees 4 factories, anon sees 0) ✓ | FND-001/002/003 live; EV-001 base |
| **B2 ✓ COMPLETE (2026-07-11)** | Live admin screens: overview, regulations, items, packages (designer read + governed publish), violations/penalties, workflows (published config v4), Risk Studio (editable, risk_owner-gated), GIS Studio, access matrix. Migration 0006: package maker-checker + approver-required trigger. Evidence B2-EV-001: draft→self-approve-blocked→distinct-approver-publish→immutable, full flow via public API with real users, auto-audited | M09 config surfaces + EV-002 chain proven |
| **B3 ✓ COMPLETE (2026-07-11)** | Golden slice executed on production (B3-EV-001): planner published visit on PKG-FS v2026.08 → journey + geofence check-in inside 150m/±4.2m with gis version stamped → 4 responses → FS-101 NC auto-fired V-FS-09/mapping v3/P-014 + mandatory photo (sha256) + blocking action form → immutable v1 + acknowledgement → duplicate retry 409 (idempotency) → post-submit edit blocked → reviewer returned exact scope [s1] → decided-review edit blocked → correction → immutable v2 → approved → Factory 360 truth query. 4 role-scoped users, public API only, 37 audit events auto-captured. /reviews screen shows it live | EV-003/005/007/008 chains proven; EV-006 (offline) lands in B5 |
| **B4 ✓ COMPLETE (2026-07-11)** | LIVE: /planning/single working wizard (CR-only search, risk badge, published-package select, inspector assignment, M01-041 validation gate with exact blockers incl. duplicate detection M02-012, atomic publish + notification, redirect to dossier) · /visits/[id] dossier (config, assignment, versions, reviews, immutable geo timeline with gis version stamps) · /factories + /factories/[id] Factory 360 (identity read-only, reproducible risk, full inspection history with violations/actions/outcomes). Migrations 0007/0008 (role visibility). PLUS: /planning/bulk (criteria filters, per-row duplicate/eligibility flags, round-robin auto-assignment, all-or-nothing publish P03) · /planning/immediate (unregistered temporary entity M01-045, mandatory location M01-046, plan-bypass M01-050) · visit lifecycle ActionBar (return w/ mandatory reason, republish same-ID, cancel final w/ pre-start guard M02-006/010, reassign w/ notification) | M01 single-path + M07 core ACs surfaced |
| **B5 ~85% (2026-07-11)** | Offline engine live (src/lib/offline.ts): IndexedDB drafts + outbox, idempotent replay (response upsert on unique key, evidence storage upsert, submission via idempotency key), explicit conflict records with keep-mine/keep-server resolver (STM-SYNC-002), sha-256 at capture, 6-state sync chip. Field screens: /field (assigned-only), /field/[visit] startup (package cache + version lock, journey, geofence check-in computing distance against LIVE gis settings, outside-fence path), /field/inspection/[id] workspace (big-target responses, config-driven NC chain surfaced, mandatory-photo capture, blockers-before-submit, offline submit queue that never lies). Private evidence bucket + storage policies. Remaining: service-worker app shell + manifest, video evidence, returned-correction field view (B8) | EV-006 mechanism live |
| **B6 ✓ COMPLETE (2026-07-11)** | Migration 0009: server-side OTP engine reading engine_settings.otp (6-digit, 5-min expiry, 3 attempts→lock even for correct code, 60s cooldown, 3 resends→audited escalation), sha-256 hashed codes, every attempt audited; DEV provider explicitly flagged (release = Unifonic adapter). Evidence B6-EV-001: full negative ladder proven live. Screens: /virtual (sessions list), /virtual/[id] room (participants, send/verify OTP via rpc, verification gates execution, begin → SAME field workspace + submission flow as physical — contract handoff). | M05 flow + EV-009 both paths proven |
| **B7 ✓ COMPLETE (2026-07-11)** | /reviews/[id] workspace: read-only snapshot render, violations/actions/evidence, prior-decision banners, decision panel (approve irreversible / return with exact section scope / reject final), auto-opens review on latest version. /operations: operational-state KPIs, live visit monitoring, immutable tracking history, open actions, notification feed | M06 decision flow + M08 monitoring live |
| **B8 ✓ CORE (2026-07-11)** | Negative sweep B8-EV-001 6/6 blocked · returned-correction flow with section locks + next-version resubmission · PWA manifest/SW app-shell/icon · remaining: full AR content pass, provider integrations | EV-011 base |
| **B9 ✓ (2026-07-11)** | B9-EV-001: 6/6 protected behaviors rerun intact; FINAL_CERTIFICATION.md issued with honest known-gaps register | EV-012 ✓ |

## Blockers
None. (Region move Seoul→Frankfurt still recommended while data is small — PAT can do it via project clone when sponsor says go. Credential rotation after build settles.)
