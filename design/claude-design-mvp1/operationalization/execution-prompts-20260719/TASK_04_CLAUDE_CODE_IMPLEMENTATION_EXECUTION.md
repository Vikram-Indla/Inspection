# Task 4 — Claude Code Execution Prompt

You implement the sponsor-accepted onboarding and Minister/Leadership designs as controlled vertical slices in the existing MIM Inspection Platform.

## Hard start gates

Do not generate or implement anything unless you can directly access `/Users/vikramindla/Developer/Inspection`, its Git worktree and mandatory authority files. Never use `/Users/vikramindla/Documents/GitHub/Inspection`.

Do not start until all of the following exist and are explicitly sponsor-approved:

1. Locked shared design system.
2. Accepted onboarding/Persona Academy addendum.
3. Accepted Minister/Leadership addendum.
4. Exact current implementation slice and change-control authority.
5. Clear file ownership boundary with Codex WP-08.

If any prerequisite is missing, return `WAITING_ON_GATE` and generate nothing.

## Read

Read `AGENTS.md`, the mandatory product-contract session-start order, current slice, open decisions, design authority, locked system, Task 1/Task 2 outputs, Task 3 accepted designs and the route/acceptance/evidence files named by the slice.

## Slice A — Onboarding and Persona Academy

Implement only approved routes, components and content models. Integrate Persona Academy into the locked shell without role switching. Implement approved media/poster/transcript/caption/audio-description/reduced-motion/slow-network/unavailable states. Use approved media or explicit placeholders only. Link learning material to routes, releases and content versions. Test every persona authorization boundary and negative state.

## Slice B — Minister/Leadership Command

Implement only within the existing approved authorization boundary. Implement governed metrics only when lineage exists. Build synchronized map/list/context behaviour, authorized preset/free-form questions, citations, filters, freshness, uncertainty, cannot-answer, partial-source and provider-unavailable states. Fail closed when AI/query/provider capability is unavailable. Keep human decisions separate and audited. Never invent a KPI, threshold, role, recommendation or production data.

## Required evidence

For each slice provide requirement/design/code/test/evidence traceability; EN/AR, RTL, light/dark, responsive and accessibility proof; authorization and negative-path tests; provider-unavailable and partial-source tests; typecheck, production build, focused tests and protected regression; before/after evidence; session and handoff records.

Use non-overlapping agents for code/test/evidence only when file ownership is explicit. One integration owner controls final integration. Do not deploy, push, merge, modify main, change schema/provider/policy/database/release state or edit the programme ledger without explicit authority.
