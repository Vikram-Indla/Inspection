# KNOWN_LIMITATIONS — v1.0
1. **Brand name resolved: SAQEEL (صقيل)** — system rebranded throughout. Still no approved logo asset (repo marks are code-drawn); the shield glyph here remains a placeholder, not a logo.
2. **Astryx repo now inventoried** — `handoff/ASTRYX_MIGRATION_TEMPLATE.md` is populated from `Vikram-Indla/Inspection@setup/Inspection` (tokens.css `--ax-*`, astryx.css `.ax-*`, components/). All five reconciliation conflicts were owner-resolved on 20 Jul 2026: the new SAQEEL spec supersedes Astryx in every case (see the decisions section there); the superseded Astryx sponsor decisions (16px body, input freeze) should be recorded as superseded in the product decision register.
3. **Map engine** — GeoWorkspace styles the chrome; the basemap/zone-hover elevation belongs to the existing engine and is only slotted, with a drawn stand-in in screens.
4. **Date/time pickers** — native inputs styled to token spec; a custom calendar popover (Hijri support, if required) is an open decision — flagged, not invented.
5. **Photo evidence imagery** — placeholder plates in screens; real photography is shown honest/untinted per spec once supplied.
6. **Four-mode coverage** — every screen family has at least one non-default-mode proof; forms/palette are token-structural in the remaining modes (see parity matrix).
7. **Hijri calendar & Arabic-Indic numerals** — defaulted to Gregorian + Latin digits for cross-referencing; confirm regulatory requirements.
