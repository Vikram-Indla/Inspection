# ACCESSIBILITY_QA_MATRIX (WCAG 2.2 AA)
Per screen × mode: text contrast ≥4.5:1 (spot-check status -text on -soft) · UI chrome ≥3:1 · focus ring visible on every interactive stop, order = DOM · Esc closes each layer, focus returns to trigger · dialogs trap focus, aria-modal + label · status conveyed by label/shape not colour alone · form errors associated (aria-describedby) + role=alert + summary links · th scope/aria-sort · touch targets ≥44px (field), ≥24px minimum elsewhere · reduced-motion collapses animation · lang/dir correct per document · identifiers announced as text (not spelled digits split) · skip-to-content first stop.
