Underline tabs for in-page sections; vertical variant for long settings/detail pages.

```jsx
<Tabs tabs={[{id:"details",label:"Details"},{id:"findings",label:"Findings",count:4}]} value={tab} onChange={setTab} />
```
