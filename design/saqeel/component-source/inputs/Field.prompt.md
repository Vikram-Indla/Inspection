Wraps any input with label, required mark, help text and inline error — the only way labels/errors are composed.

```jsx
<Field label="Permit number" required error={errors.permit}><Input mono value={v} onChange={...}/></Field>
```
