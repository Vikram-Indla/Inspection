Single-line input for every text-like type; mono flag for machine identifiers (stays LTR inside Arabic).

```jsx
<Input type="search" icon={<SearchIcon/>} placeholder="Search register" />
<Input mono placeholder="INS-2026-000000" />
```
Native date/time types cover date picker, time picker; pair two for a range.