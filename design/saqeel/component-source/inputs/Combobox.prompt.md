Combobox covering autocomplete, searchable select and multi-select (chips). Full keyboard support: arrows, Enter, Esc, Backspace-to-remove.

```jsx
<Combobox multi options={inspectors} value={sel} onChange={setSel} placeholder="Assign inspectors" />
```
