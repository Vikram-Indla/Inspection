Dropdown of canonical StatusBadges — status changes always pick from the fixed vocabulary.

```jsx
<StatusSelector value={status} onChange={setStatus} statuses={["pending","onhold","completed"]} />
```
