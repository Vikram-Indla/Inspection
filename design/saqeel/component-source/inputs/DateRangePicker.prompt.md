Date range on paired native date inputs (cross-constrained min/max). The arrow flips under RTL via a data-directional convention at implementation.

```jsx
<DateRangePicker from={f} to={t} onChange={setRange} />
```
