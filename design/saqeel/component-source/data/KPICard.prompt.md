Hero metric panel (the only 700-weight type). Direction colours the delta, not the value, unless the value IS the state.

```jsx
<KPICard label="Open inspections" value="1,284" delta="+42" deltaDirection="up" caption="vs last week" />
```
