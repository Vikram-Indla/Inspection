Side panel for contextual detail (selected inspection, filters, geographic context) — keeps the workspace visible behind it.

```jsx
<Drawer open={!!selected} title="Inspection detail" onClose={clear}>…</Drawer>
```
