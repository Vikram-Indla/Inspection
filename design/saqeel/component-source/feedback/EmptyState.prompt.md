States what is absent + one next action. Covers empty, no-results, error, permission-denied and offline by swapping copy/icon.

```jsx
<EmptyState title="No inspections match these filters" action={<Button variant="secondary">Clear filters</Button>}>Try widening the date range.</EmptyState>
```
