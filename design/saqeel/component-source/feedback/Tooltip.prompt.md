Hover/focus tooltip — supplements, never replaces, a visible label. Required on collapsed-sidebar icons and truncated cells.

```jsx
<Tooltip label="Notifications"><Button variant="ghost" iconOnly aria-label="Notifications" icon={<BellIcon/>} /></Tooltip>
```
