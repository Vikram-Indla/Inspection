Status-coloured markers (facility / vehicle / inspector / zone) with white keylines for basemap visibility, plus MapCluster for grouped counts. Never tint the basemap.

```jsx
<MapMarker kind="vehicle" tone="compliant" label="Unit 12" /> <MapCluster count={24} />
```
