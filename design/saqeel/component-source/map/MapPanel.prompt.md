Floating map surface (96% opaque + blur) hosting legend, layer control, filters or selected-context detail. Exports MapLegend and MapLayerControl too.

```jsx
<MapPanel title="Layers" position="end" width={220}><MapLayerControl layers={layers} onToggle={t} /></MapPanel>
```
