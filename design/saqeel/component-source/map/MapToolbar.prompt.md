Centered floating toolbar (search + filters + actions) and MapZoom (zoom / locate stack, block-end inline-end).

```jsx
<MapToolbar search={<Input type="search" placeholder="Search zones or facilities" />} /> <MapZoom onIn={z} onOut={z} onLocate={l} />
```
