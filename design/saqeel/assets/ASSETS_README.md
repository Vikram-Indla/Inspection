# ASSETS
No approved brand assets exist yet: no official SAQEEL logo, no approved illustrations, no photography. The shield glyph in screens is a code-drawn placeholder (see foundations/BRAND_AND_IDENTITY.md).
Map/inspection/vehicle markers are COMPONENT-DRAWN (component-source/map/MapMarker.jsx — Lucide glyphs in status-toned keyline circles), not image assets, so they theme and scale correctly; do not rasterize them.
Icons: Lucide via package/CDN at implementation (stroke 1.5) — no icon files shipped by design.
When brand supplies the approved logo + clear-space rules, add them here and replace the placeholder wordmark.