# IPAD_APPLICATION_SHELL
- ≤1024px: sidebar auto-collapses to 60px icon rail (tooltips); 768 portrait: drawer navigation from the menu button.
- TopBar keeps: menu, search (collapsible to icon <900px), EN/AR, theme, notifications, avatar — all ≥44px.
- Field surfaces add a field taskbar region (sticky block-end) carrying the primary action + SyncIndicator.
- Reference: screens/tablet (834px, field density, offline chrome).