# Saqeel V2 — admin patterns
Reference built: patterns/admin/approval-queue.html (.ax-density-compact). Rules: compact 36/40px controls, 14/20 type, dense tables with row-identity selection labels + sort buttons, filter toolbar (chips + saved views), maker-checker approval queue with diff links, version comparison via ax-diff-*, no card grids — flat sections + rules, danger actions in overflow/danger zones only. All admin references built: patterns/admin/ approval-queue, template-editor, rule-builder, workflow-designer, permissions, version-comparison. Regression specimens: patterns/gallery.html.