# Saqeel V2 — web patterns
Reference built: patterns/web/visit-planning.html (redesign of the critiqued specimen at page scale). Patterns: hybrid shell (rail + command bar w/ approved texture), page header (breadcrumb+title+chips+1 primary), status rail, command bar (search+scope+chips, 1 primary), priority-column tables w/ sort buttons + identity row labels, metric strip (no KPI boxes), record rows for versions, governed decision flows (confirmation restates version/scope/reason). Dashboard/dossier/review compositions follow the approved A1/B2 hybrid (explorations/premium-pilot). All web references built: patterns/web/ visit-planning, dashboard, factory-dossier, review-workspace (dark-theme reference), report-reading (A1 editorial renderer).