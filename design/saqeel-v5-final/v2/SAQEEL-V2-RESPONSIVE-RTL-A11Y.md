# Saqeel V2 — responsive, RTL, accessibility
Carried from explorations/premium-pilot/SAQEEL-PREMIUM-RESPONSIVE-RTL-SPEC.md, now canonical. V2 additions: deterministic shell modes incl. iPad landscape/portrait/Split View; scroll-margin tokens applied globally ([id]{scroll-margin…}); control boundaries ≥3:1 in both themes via --ax-color-border-control; status rail wraps to numbered ledger ≤900px; 400% zoom = single-column reflow, tables get labeled keyboard-scrollable overflow. Dates: Gregorian primary, Asia/Riyadh, Hijri = org-controlled optional suffix (OFF, pending legal). RTL: logical properties, <bdi> on all technical values, من/إلى ranges.