/** Version badge — every reviewed artifact shows exact version (ENG-09/EV-008). Mono type, ⎘ glyph. */
export interface VersionBadgeProps { children?: React.ReactNode; className?: string; }
export declare function VersionBadge(props: VersionBadgeProps): JSX.Element;
