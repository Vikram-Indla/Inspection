Astryx button (V2). One principal action per zone; Cancel = secondary; audit/history = subtle; destructive entry = variant="danger-subtle" or overflow menu; variant="danger" (solid) ONLY in confirmation dialogs. Loading keeps its label. Disabled must expose a reason.\n\n```jsx\n<Button>Publish version</Button>\n<Button variant="secondary">Cancel</Button>\n<Button variant="subtle">View audit</Button>\n<Button variant="danger-subtle">Delete draft</Button>\n<Button loading>Publishing version…</Button>\n```\n\nSizes: default 44px principal · size="field" 52px iPad. Admin surfaces set .ax-density-compact on a root instead of per-button overrides.