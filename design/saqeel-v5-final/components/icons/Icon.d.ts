/** Saqeel stroke icon (24px grid, 1.8 stroke, currentColor).
 * @startingPoint section="Primitives" subtitle="Production stroke icon set" viewport="700x150" */
export interface IconProps {
  /** Glyph name: dashboard | radar | factory | calendar | visits | inspect | virtual | review | admin | library | forms | enforcement | workflow | risk | map | access | notify | insights | search | lock | close | menu | chevronDown | check | target | shield | eye | video | mapPin | fingerprint | link */
  name: string;
  size?: number;
  className?: string;
}
export declare function Icon(props: IconProps): JSX.Element;
