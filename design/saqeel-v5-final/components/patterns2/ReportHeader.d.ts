/** Official report header — bilingual ministry identity band; compresses to the 18mm repeating print running header. */
export interface ReportHeaderProps { reference?: string; version?: string; className?: string; }
export declare function ReportHeader(props: ReportHeaderProps): JSX.Element;
