/** Bulk selection action bar (dark, appears above table on selection). */
export interface BulkBarProps { count: number; label?: string; children?: React.ReactNode; className?: string; }
export declare function BulkBar(props: BulkBarProps): JSX.Element;
