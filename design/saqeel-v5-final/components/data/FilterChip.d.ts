/** Filter chip — dashed when available, solid primary when active. */
export interface FilterChipProps extends React.ButtonHTMLAttributes<HTMLButtonElement> { active?: boolean; }
export declare function FilterChip(props: FilterChipProps): JSX.Element;
