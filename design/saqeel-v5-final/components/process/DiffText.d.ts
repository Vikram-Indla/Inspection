/** Inline diff mark for version comparison (SCR-WEB-320, ENG-09). */
export interface DiffTextProps { type?: "ins" | "del"; children?: React.ReactNode; }
export declare function DiffText(props: DiffTextProps): JSX.Element;
