# Handoff Validation Matrix — SAQEEL-DSYNC-001

> **CORRECTION 2026-07-24 (Codex re-review):** row 2 below ("97 vs 98 — off
> by exactly one... not further resolvable") is RESOLVED, not unconfirmed.
> Root cause: `SAQEEL Executive Overview.html` is a plain `.html` page (no
> `.dc.html` twin), missed by the original `.dc.html`-only filter. 97 + 1 =
> 98, exact match. See `reports/inventory-reconciliation.md`.

Source pack: `SAQEEL_Claude_Code_Design_Sync_Handoff.zip` (extracted
`/private/tmp/claude-502/.../scratchpad/design-sync-handoff/`). All 10 doc
files + 1 evidence PNG read in full. MANIFEST.json checksums not
independently re-verified (not required for this read-only pass).

Scope note: per explicit sponsor decision this session, only Phases 1–11 of
the handoff's execution contract are in scope. Phases 12–20 (approved
implementation, watcher daemon, `launchd` install, project-local skill files,
continuous-workflow demonstration) are **not attempted** — they require either
application-code consent (Gate G5) or a routed product-contract task, neither
of which exists yet. This matrix reflects that boundary.

| # | Requirement (source file) | Repo/environment evidence | Status | Disposition |
|---|---|---|---|---|
| 1 | "SAQEEL Design System" project exists, ~98 pages (`README_START_HERE.md`, `SOURCE_CONTEXT.md`) | 3 Claude Design projects match candidate names: `SAQEEL Design System` (5e8154ad), `Saqeel` (b3b548d2, 3 dc.html files only), `Saqeel Design System` (49c57df3, component-library project, no page-like content). Only 5e8154ad has 97 top-level `.dc.html` pages + a `SAQEEL PWA-Field Login.dc.html`. | **Supported, disambiguated** | Use project `5e8154ad-aa9e-4e3d-9b7a-c66ca020bd61` as the sole target. The other two "Saqeel"-named projects are decoys/legacy and must not be touched. |
| 2 | Page count reconciles with 98 shown in screenshot | 97 top-level `.dc.html` files found via `list_files(depth=-1)`. 81 additional `.dc.html` files exist nested under `export-claude-code-sync/`, `export-inspector-pack/`, `export-non-inspector/` — these are point-in-time export snapshots, not live pages. | **Partially supported** | 97 vs 98 — off by exactly one. Most likely explanation: the screenshot's page-picker count included the project's own root/canvas entry (`Canvas.dc.html` is present and counted here as a page but may not be a "page" in the picker's sense), or one page was added/removed between screenshot capture and this session (file mtimes span 2026-07 continuously, consistent with an actively-edited project). Not further resolvable without the original screenshot's exact timestamp — logged as unconfirmed, not blocking. |
| 3 | Pilot page is "SAQEEL Field Login" (`README_START_HERE.md`, `SOURCE_CONTEXT.md`) | No file is named exactly `SAQEEL Field Login.dc.html`. The matching page is **`SAQEEL PWA-Field Login.dc.html`** (17,590 bytes, most-recently-edited of all login-adjacent files: etag/mtime `1784884213895548`). A separate, unrelated `SAQEEL Login.dc.html` (15,163 bytes) also exists — this is the **web console** login, out of scope. | **Contradicted by repository evidence, resolved** | Handoff's exact filename claim is wrong; the intended page is `SAQEEL PWA-Field Login.dc.html`. Application code already treats this exact file as source of truth (see `apps/web/src/app/login/field/page.tsx` header comment, same project ID cited). |
| 4 | Claude Design MCP provides stable page/node IDs, revision metadata (`CLAUDE_CODE_EXECUTION_CONTRACT.md` Gate G1, `STATE_AND_EVIDENCE_TEMPLATES.md` §2) | `mcp__claude-design__list_files` exposes only `path`, `type`, `size`, and an opaque `etag` (a numeric string, monotonically increasing per edit — behaves like a version stamp but is undocumented as such). No node-level IDs, no component/layer IDs, no revision history endpoint, no diff-since-revision capability. | **Partially supported** | `etag` is usable as a page-level revision/hash proxy (Gate G3's "deterministic hash test" can be satisfied by hashing `read_file` content + etag). No node-level identity exists — semantic diffing below page granularity must derive stable IDs from content structure (e.g. DOM `id`/`data-*` attributes inside the `.dc.html`), not from the MCP. |
| 5 | Native change-event/webhook capability (`CLAUDE_CODE_EXECUTION_CONTRACT.md` Gate G1, `MASTER_PROMPT` Phase 4/16) | No subscription, webhook, or push-event tool exists among `mcp__claude-design__*`. Only pull-style reads (`list_files`, `read_file`, `list_comments`, `get_conversation`). | **Absent** | Confirmed no native event mechanism. Any detector must be poll-based (`list_files` diff by etag), exactly as the handoff's own fallback describes (`BOOTSTRAP_AND_DAILY_COMMANDS.md` §6, macOS `launchd`, 300s interval). Not built this session (out of Phase-1–11 scope; `launchd` install requires no consent gate per the handoff itself, but was excluded by explicit sponsor scoping this turn — see cover note above). |
| 6 | `/consent` skill exists and should be reused, not shadowed (`CLAUDE_CODE_EXECUTION_CONTRACT.md` Gate G2, `ACCEPTANCE_CRITERIA.md` §C) | Searched project `.claude/` and user `~/.claude/` for any file/skill matching `consent`, `design-login`, `design-sync`. **None exist.** The full skill listing available in this session (40+ skills) contains no such names. | **Contradicted by handoff's assumption** | `/consent` does not exist in this Claude Code install. Per the handoff's own instruction ("When it does not exist, propose and create a project-local consent skill only after documenting that distinction" — `MASTER_PROMPT` Phase 11), a project-local consent mechanism is required. **Not created this session** — creating a new skill is a repo-affecting change beyond the read-only research scope the sponsor approved; the consent packet below (Phase 11 deliverable) is produced as a plain Markdown artifact requiring manual sponsor sign-off instead. |
| 7 | Field Login screenshot claims: trusted-device biometric, untrusted-device OTP challenge, resend timer, "Trust this device", OTP delivery channel unwired (`SOURCE_CONTEXT.md`) | Design file (`SAQEEL PWA-Field Login.dc.html`) confirms all of these as **mockup-only, client-state-simulated** behavior (`this.setState`, hardcoded OTP digits `4,9,,,,,`, hardcoded device id `DVC-77120`, hardcoded "Last sync 08:41", and the DC's own English copy states plainly: *"Delivery channel (SMS / email / authenticator) is set during enrollment — not wired in this mockup."*). Live implementation (`FieldLoginClient.tsx`) implements **only** the trusted-device Face ID unlock path (gated on a real backend device-trust register + local WebAuthn opt-in) and a plain-credential form; it deliberately does **not** implement the OTP/unrecognized-device challenge at all — see its own header comment documenting this as a corrected security model, not an oversight. | **Implemented differently, with documented rationale** | This is not a gap to fill by default — it's a considered divergence. See Phase 12 report and consent packet for the exact classification and the decision this surfaces (align design to code vs. build OTP). |
| 8 | Repository/target ambiguity resolution required before any write (`CLAUDE_CODE_EXECUTION_CONTRACT.md` Gate G0) | Monorepo at `/Users/vikramindla/Developer/Inspection`, remote `github.com/Vikram-Indla/Inspection`, Next.js app at `apps/web/`, field login route at `apps/web/src/app/login/field/`. No competing repository candidate found. | **Supported** | Single unambiguous target repo. |
