# Claude Capability Audit — SAQEEL-DSYNC-001

| Capability | Handoff assumption | Actual state this session | Usable for this workflow |
|---|---|---|---|
| `/consent` | Official or pre-existing project skill (`CLAUDE_CODE_EXECUTION_CONTRACT.md` Gate G2) | **Does not exist.** No match in project `.claude/`, user `~/.claude/`, or the full skill listing available this session. | No — must be a project-local Markdown-artifact-plus-manual-signoff mechanism until/unless built as a real skill (a repo-affecting change, out of this read-only pass's scope). |
| `/design-login` | Assumed present (`ACCEPTANCE_CRITERIA.md` §B) | Does not exist as a skill. Claude Design access is instead via the always-available `mcp__claude-design__*` MCP tool family (`list_projects`, `get_project`, `list_files`, `read_file`, `list_comments`, `get_conversation`), authenticated implicitly through this Claude Code session — no separate login skill needed or found. | The MCP tools themselves are usable and were used (see Gate G1 evidence in the handoff-validation-matrix). |
| `/design-sync` | Assumed present (`ACCEPTANCE_CRITERIA.md` §B) | Does not exist. Nothing in the skill listing or `.claude/` matches. | No — the 8 `/saqeel-design-*` skills the handoff asks to be created (`CLAUDE_CODE_SKILLS_TO_CREATE.md`) are not built this session; out of the sponsor-approved Phase 1–11 research scope (writing new skill files is a repo change). |
| Claude Design MCP (`mcp__claude-design__*`) | Primary design-access mechanism | Present and functional. Verified: `list_projects` (21 projects returned), `get_project`, `list_files` (depth -1, up to 1440 entries in one project), `read_file` (256 KiB cap, entity-escaped, etag-bearing). **Not exercised this session** (no writes needed): `write_files`, `copy_files`, `delete_files`, `create_project`, `render_preview`, `ack_comments`, `put_conversation`, `create_support_js`, member/sharing tools. | Yes, read-only surface is sufficient for discovery/inventory/diff-input. No native event/webhook tool exists in this family — confirmed absent, not just unconfirmed. |
| Figma MCP (`mcp__2d8919de-...__*`) | Not part of this handoff (Claude Design only) | Present, connected, unrelated to this pack — the design source of truth here is Claude Design, not Figma. Not used. | N/A |
| Supabase MCP | Not part of this handoff | Present but **requires authentication the user must grant interactively** (listed among servers needing `claude mcp`/`/mcp` auth). Not used — device-trust register verification relied on repo source code (`actions.ts`), not live DB queries. | Blocked pending user auth if live-DB verification of `field_device` rows is ever wanted. |
| Playwright / browser runtime verification | Assumed available for Phase 14 | `apps/web` already ships 6 Playwright configs including a UI-compliance/visual-regression harness (`playwright.ui-compliance.config.ts`, `playwright.inspector-visual.config.ts`). **Not run this session** — Phase 14 (runtime verification) is downstream of implementation consent (Gate G5/G6), which has not been granted. | Confirmed available for whenever implementation is approved; reuse existing configs rather than authoring new ones. |
| Project-local hooks / CLAUDE.md governance | N/A in handoff | `CLAUDE.md` at repo root enforces: no `main` push without approval, no invented policy values, gate-based broad-implementation authorization (currently G0–G9 PASS, G10 in progress). This design-sync work item is **not** a routed task in `product-contract/execution/TASK_ROUTER.yaml` or `CURRENT_SLICE.yaml`. | Governs everything above Phase 11 — implementation cannot proceed under this repo's own rules without either (a) a routed product-contract task, or (b) explicit ad-hoc sponsor approval scoped narrowly like this session's own `AskUserQuestion` gate did. |
| `Workflow` multi-agent orchestration tool | Handoff's `CLAUDE_CODE_AGENT_PLAN.md` describes 10 bounded agents | Available, but per this session's own operating rules requires explicit user opt-in ("ultracode" keyword or explicit ask), which was not given. | Not invoked. The read-only discovery in this session was done directly rather than via the full 10-agent topology; findings are equivalent in content, not in mechanized parallelism. If a sponsor wants the agent topology as specified for a future consent-approved implementation pass, it can be built as a `Workflow` script at that time. |
